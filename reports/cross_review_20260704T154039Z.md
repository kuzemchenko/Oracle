# Кросс-вендорное ревью 20260704T154039Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №7 (финальный по challenge.py). Закрыто из №6: (1) имя протокола клеймится os.O_EXCL — гонка двух писателей физически не даёт одно имя, затем атомарная замена; (2) матч тикера двусторонний по канону .US (AAPL↔AAPL.US), точечные тикеры (BRK.B) не срезаются; (3) since — UTC-нормализация. Тесты добавлены. За 6 прогонов закрыто: 2 BLOCKER + 10 HIGH. Финальный вердикт.
Файлы: orchestrator/challenge.py, orchestrator/tests/test_challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:248 — инъекция котировок/индикаторов из `oracle.db` пропускается, если `build_context()` уже положил ключ актива со значением `None`/пустышкой — сценарий: `C.build_context("CLF.US")` возвращает `{"quotes": {"CLF.US": None}, "indicators": {"CLF.US": None}}`, при этом в `oracle.db` есть бары `CLF.US`; условие `sym not in (ctx.get("quotes") or {})` ложно, `C._quotes(con, sym)` не вызывается, в дебаты уходит `quotes["CLF.US"] = None`, и судья получает ложное «нет данных» вместо имеющейся истории. Это прямо ломает заявленное закрытие H7 и П8.

- [LOW] orchestrator/challenge.py:80 — валидный JSON неправильной формы в журнале валит поиск идей, а не пропускается — сценарий: в `logs_dir` лежит `bad.json` с содержимым `[]`; `_scan_protocols()` добавляет список в `out`, затем сортировка вызывает `_ts_key(pr)` / `pr.get(...)`, что даёт `AttributeError: 'list' object has no attribute 'get'`. Один такой файл блокирует `find_idea()`/`list_ideas()`, хотя битые JSON уже предполагается терпимо пропускать.