# Кросс-вендорное ревью 20260704T155918Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №16 (АРБИТРАЖНЫЙ ФИНАЛ по challenge.py). Закрыто из №15: _TS_RE принимает Z-опционально. №15.2 отклонено: Z-суффикс — общепроектная конвенция run_id, ключи нормализуются; №15.3 отклонено: write=False — явный контракт не-записи (тесты/dry). Прошу финальную оценку С УЧЁТОМ калибровки: за 15 прогонов закрыто 2 BLOCKER + 21 HIGH; проект — один сервер, один однопоточный бот, все журналы пишет свой код. Если остались находки ТОЛЬКО класса «фикстура с экзотическим именем/двойной контейнер pid=1» — это LOW-бэклог, вердикт ПРОЙДЕН.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:252 — ранние отказы не журналируются при `write=True` — сценарий: пользователь вызывает `run_challenge("сомнение", asset="TYPO", write=True)`, получает `{"ОТКАЗ": "идея не найдена", ...}`, но файл в `journal/challenges/` не создаётся, `run_id/ts/mode` отсутствуют. Это ломает аудит попыток разбора, хотя бюджетные отказы ниже уже специально пишутся как аудируемые.

- [HIGH] orchestrator/challenge.py:396 — `digest_challenges()` считает live-бюджетные отказы/остановы как полноценные live-разборы — сценарий: в `journal/challenges/challenge_x.json` лежит результат precheck-отказа:
  `{"mode":"live","ОТКАЗ_бюджет":{...},"резюме":"..."}`. Дайджест добавит его в `n_разборов`, положит `по_вердикту[None] += 1`, создаст запись с `вердикт=None`. В результате `/review-week` получает статистику по несуществующему дебату и может делать выводы из отказов, а не из разборов.

- [HIGH] orchestrator/challenge.py:361 — `_rubric_scores()` падает на не-dict элементе в `рубрика.оценки` — контрпример в live-журнале:
  `{"mode":"live","дебаты":{"реплики":{"судья":{"judgment":{"рубрика":{"оценки":["нет данных"]}}}}}}`.
  При вызове `digest_challenges()` будет `AttributeError: 'str' object has no attribute 'get'`; один кривой LLM/старый журнал валит весь недельный дайджест.

- [HIGH] orchestrator/challenge.py:372 — `_missing_findings()` падает на не-dict элементе в `reviewer_данных.judgment.находки` — контрпример:
  `{"mode":"live","дебаты":{"реплики":{"reviewer_данных":{"judgment":{"находки":["нет данных по выручке"]}}}}}`.
  Код вызывает `f.get(...)` у строки и валит `digest_challenges()`. Это не экзотика имени фикстуры, а типичный дрейф LLM-структуры/старого журнала.