# Кросс-вендорное ревью 20260704T144744Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П3 «Сессия партнёра» (§17.6, REVISION_2026-07 §R3, подписано): детерминированная упаковка research-потока в диалог. partner_session.py собирает 3-5 идей из ПОСЛЕДНЕГО ef_-протокола в СУЩЕСТВУЮЩЕМ порядке выдачи (money→провизорный→картограф) — собственного ранжирования НЕТ (граница П2б, слой Б). Журнал сессий append-only; метрики §R5 (retention, докапывания, выживаемость по challenges). Бот: /session + интенты. Ищи: скрытое ранжирование/фильтрацию (нарушение границы П2б), нечестность П8 (выдуманные поля, подмена устаревшего), дефекты метрик (naive/aware, деление на 0, окна), конфликты интентов, крэши на кривых протоколах.
Файлы: orchestrator/partner_session.py, orchestrator/tests/test_partner_session.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] orchestrator/partner_session.py:137-146; orchestrator/tests/test_partner_session.py:74-79 — устаревший протокол не блокируется, а упаковывается в полноценную сессию с идеями — контрпример: протокол `ts="2026-06-20T09:00:00+00:00"`, `asof="2026-07-04T18:00:00+00:00"` даёт возраст ≈14.38 дня, код ставит `"прогон_устарел": true`, но всё равно возвращает `идеи` и `идей=3`; это «греем вчерашнее», а не честное предложение свежего прогона. Тест прямо закрепляет это поведение.

- [HIGH] orchestrator/partner_session.py:38-43 — `load_latest_protocol()` молча подменяет последний `ef_`-протокол предыдущим валидным, если последний битый — сценарий: в каталоге есть `ef_2026-07-04.json` с невалидным JSON и `ef_2026-07-01.json` валидный; функция вернёт `ef_2026-07-01.json`, хотя задание требует идеи из ПОСЛЕДНЕГО `ef_`-протокола. Честный ответ должен быть «последний протокол нечитаем / нет данных», а не скрытая подмена.

- [HIGH] orchestrator/partner_session.py:124-136 — скрытая фильтрация/выбор по `seen` и `актив`, не заявленная в задании «собирает 3–5 идей в существующем порядке, собственного ранжирования/фильтрации нет» — контрпример: протокол содержит 3 идеи в порядке выдачи: money `A.US`, провизорный `A.US` с другим направлением/цепочкой, картограф `B.US`; код вернёт только `A.US`, `B.US`, то есть 2 идеи и `"мало_идей": true`, хотя в последнем протоколе было 3 идеи. Фраза в комментарии «первое вхождение богаче» — это собственное качественное предпочтение.

- [HIGH] orchestrator/partner_session.py:122,126,130,134; orchestrator/partner_session.py:65; orchestrator/partner_session.py:100 — крэши на кривых, но синтаксически валидных протоколах — контрпримеры:  
  - `protocol={"граф_отбор": []}` → `g.get(...)` падает с `AttributeError`;  
  - `protocol={"граф_отбор":{"money_трек":[42]}}` → `brief.get(...)` падает;  
  - `protocol={"граф_отбор":{"суд_money": []},"картограф_идеи":[]}` → `суд.get(...)` падает;  
  - `protocol={"картограф_идеи":[{"актив":"X.US","узлы_каскада":[1]}]}` → `n.get(...)` падает.  
  Для `/session` это означает отказ сервиса вместо честного «протокол повреждён / поле нечитаемо».

- [HIGH] orchestrator/partner_session.py:220-223 — метрика выживаемости занижает/искажает исход `УСТОЯЛА_БЕЗ_КРИТИКА`: код включает его в `judged`, но не включает в `survived` — контрпример: один challenge в окне с `{"исход":"УСТОЯЛА_БЕЗ_КРИТИКА"}` даст `judged=1`, `survived=0`, `"выживаемость": 0.0`. Если это «устояла», числитель должен быть 1; если это не полноценный verdict, его нельзя включать в знаменатель.

- [LOW] orchestrator/partner_session.py:228 — `retention_ok` вычисляется как `n_sessions >= 1` для любого `days`, хотя в комментарии и задании KPI — «≥1 сессии/нед» — контрпример: `session_metrics(asof=..., days=30)` и одна сессия 29 дней назад вернут `"retention_ok": true`, хотя недельный retention за последнюю неделю равен 0 сессий.

- [LOW] orchestrator/partner_session.py:157-166 — `record_session()` может записать отказ как факт сессии, если вызывающий код передаст результат `build_session(None, ...)` — контрпример: `record_session({"ОТКАЗ":"нет протокола"})` добавит строку с `"ts": null`, `"run_id_источника": null`, `"идей": null`, `"активы": []`. Это загрязняет append-only журнал несуществующей сессией; метрики потом такую строку молча пропустят, но журнал уже нечестный.