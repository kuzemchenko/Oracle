# Кросс-вендорное ревью 20260704T141616Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П2а (REVISION_2026-07 §R4.2, подписано владельцем): поле «внимание» на идеях выдачи event_first — ИНФОРМАЦИОННОЕ (на ранжирование/отбор НЕ влияет; пере-ранжирование = П2б отдельно). Новый модуль attention_field.py: сиды → append-only реестр с ЗАПРЕТОМ пересдачи ключа → кандидаты из слов новостного кластера (детерминированно, журналируемый провенанс). Правила честности §R0#5: нет данных → «не_измерено» с причиной (свежесть=None ≠ 1.0). Судья видит поле в деле каскада (вход тайминга §8). Покрытие §R5 в протоколе. Ищи: способы обойти запрет пересдачи, места где None превращается в награду/штраф, влияние на ранжирование, гонки на реестре, поломку фетча/дайджеста, дефекты детерминизма.
Файлы: orchestrator/attention_field.py, config/attention_keys.yaml, orchestrator/tests/test_attention_field.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] orchestrator/attention_field.py:72-80 — запрет пересдачи ключа не атомарен, реестр пишется без блокировки/транзакции — сценарий: два процесса одновременно вызывают `assign_key("CCJ.US", "uranium squeeze", ...)` и `assign_key("CCJ.US", "uranium", ...)`; оба до записи видят пустой реестр на строке 73, оба дописывают разные записи на строках 78-79. Второй вызов возвращает свой новый ключ и может посчитать поле по нему в текущем прогоне, хотя позднее `_load_registry()` будет «первым выигрывает». Контракт «пересдача запрещена, первый назначенный ключ окончательный» нарушен.

- [HIGH] orchestrator/attention_field.py:83-91 — seed из `config/attention_keys.yaml` всегда имеет приоритет над уже назначенным ключом из append-only реестра — контрпример: в день 1 для `CCJ.US` журналируется ключ `"uranium squeeze"`; в день 2 в `attention_keys.yaml` добавляют `CCJ.US: "uranium"`. `resolve_key()` на строках 87-88 вернёт новый seed и проигнорирует первый журнальный ключ. Это фактическая пересдача ключа без записи в append-only реестр.

- [HIGH] orchestrator/attention_field.py:106-112 — публичный параметр `registry` позволяет обойти журналируемый провенанс — сценарий: вызвать `field_for_asset(..., asset="CCJ.US", registry={"CCJ.US": {"ключ": "удобный ключ", "источник": "manual", "run_id": "x"}}, registry_path=пустой_файл)`. `resolve_key()` примет подложную запись, `assign_key()` не будет вызван, в `journal/attention_keys.jsonl` ничего не появится, но поле будет посчитано по незарегистрированному ключу.

- [HIGH] orchestrator/attention_field.py:54-60 — один битый JSONL-ряд ломает загрузку всего реестра, а значит может сломать фетч/дайджест — контрпример: из-за падения процесса или ручной правки в `journal/attention_keys.jsonl` появилась строка `{"актив":"CCJ.US","ключ":`. `_load_registry()` на строке 59 бросит `json.JSONDecodeError`; `registry_keywords()` и `annotate_ideas()` не имеют обработки, весь прогон падает вместо честного `не_измерено` по проблемной записи.

- [LOW] orchestrator/attention_field.py:107-110 — детерминизм выбора кандидата зависит от типа/порядка `candidates`, модуль это не фиксирует — контрпример: если вызывающий передаст `candidates={"uranium", "uranium squeeze"}` как `set`, то `next(...)` может выбрать разные ключи при разных `PYTHONHASHSEED`; первый выбранный ключ затем навсегда попадёт в реестр.

- [LOW] orchestrator/tests/test_attention_field.py:103 — тест инварианта «поле не влияет на ранжирование» содержит бессмысленную проверку `... or True` — контрпример: даже если `s["внимание"]["score"] == s["score"]` или тестовая логика ошибочна, assert всегда проходит; дефект влияния через вложенный score этим тестом не ловится.