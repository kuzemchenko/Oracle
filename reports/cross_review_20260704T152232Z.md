# Кросс-вендорное ревью 20260704T152232Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ фиксов (не этап карты, но кросс-контроль обязателен). Изменения: (1) cascade._lag_for_pair — направленная семантика (directed-first, undirected-фолбэк); (2) portfolio — честный охват месячного лимита + monthly_spent_usd; (3) RunBudgetGuard.unaccounted_calls; (4) ops/cron_alert.sh + бот-пуш алертов; (5) M13 атомарные протоколы (progress.atomic_write_text); (6) sqlite timeout=30 ×12; (7) M2 честный ОСТАНОВ-md; (8) M9 _ts_key нормализация; (9) M12 direction_to_side токены+отрицание, _sign синонимы/?; (10) M15 XSS-экранирование; (11) M11 cascade_edge None; (12) M8 build_context(asof)→NotImplementedError; (13) replay: build_from_db(asof) + run_replay (без LLM/seal, replay_logs). Ищи ТОЛЬКО BLOCKER/HIGH с контрпримером на боевом пути: сломанные вызыватели изменённых сигнатур, регрессии семантики, дыры П16 в replay, гонки.
Файлы: mathlib/cascade.py, mathlib/portfolio.py, orchestrator/run_budget.py, orchestrator/forecast.py, orchestrator/challenge.py, orchestrator/cascade_build.py, orchestrator/event_first.py, ops/cron_alert.sh

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] mathlib/cascade.py:265 — `_lag_for_pair` не реализует заявленную семантику directed-first: первая запись с `pair == [a,b]` возвращается сразу, даже если она ненаправленная, и более поздний directed-overrides уже не рассматривается — контрпример: `links=[{"pair":["A","B"],"lag_days":0}, {"pair":["A","B"],"directed":True,"lag_days":3}]`; вызов `_lag_for_pair("A","B",links)` вернёт `0`, хотя directed-first должен вернуть `3`. На боевом пути `cascade_from_quotes()` применит неверный лаг и посчитает чувствительность/амплитуду на неправильном сдвиге.

- [HIGH] orchestrator/run_budget.py:148 — `RunBudgetGuard.unaccounted_calls` только считает вызовы без `cost_usd`, но не ограничивает их и не начисляет даже prior-cost; такие live-вызовы остаются фактически бесплатными для стопа-на-лету — контрпример: cap `$1`, 100 live LLM-вызовов возвращают `usage.cost=None`; `guard.add(None)` 100 раз даст `spent_usd=0`, `unaccounted_calls=100`, исключение не будет брошено, хотя реальные траты могут давно превысить лимит. Это дыра бюджетного контура на production path `event_first -> _cartographer_pass/_vet_money -> client.cost_guard`.

- [HIGH] orchestrator/event_first.py:548 — в live `run_event_first()` корневой shock режется `asof=now`, но `CB.build_from_db(... db=DB)` вызывается без `asof=asof_date`, поэтому terminal `realized/vol` берутся из полного хвоста БД — контрпример: сегодня `2026-07-04`, в `quotes` по терминалу случайно уже лежит бар `2026-07-05` после досинка/таймзонной ошибки; root shock считается по `date<=2026-07-04`, а `cascade_build.realized_fn()` внутри `build_from_db` использует будущий бар `2026-07-05`, меняет `edge/probability`, после чего `seal_predictions=True` может запечатать прогноз с look-ahead.

- [HIGH] orchestrator/cascade_build.py:225 — `build_from_db(asof=...)` честно режет `realized/vol/has_data`, но `sensitivity_fn()` игнорирует `asof` и вызывает `SEN.on_the_fly(up, down, lag=lag, db=db)` по полной истории; `run_replay()` затем маркирует это как граф “как был бы на дату cutoff” — контрпример: на `cutoff=2024-01-01` у ребра `A→B` было 40 наблюдений и оно не могло быть tier A, но к 2026 накопилось 300 наблюдений и ребро стало pinned с `beta_pinned`; replay за 2024 построит money/provisional-отбор с будущей чувствительностью. Это P16 look-ahead в `journal/replay_logs`, особенно опасно как сырьё для последующего тюнинга.

- [HIGH] orchestrator/challenge.py:176 — протоколы challenge пишутся неатомарно через `write_text()`, а `run_id` имеет точность до секунды; параллельные `/debate`-запросы в одну секунду получают одинаковый `challenge_YYYYMMDDTHHMMSSZ` и пишут один и тот же файл — контрпример: два API-запроса `run_challenge()` одновременно в `12:00:00Z`: оба пишут `journal/challenges/challenge_20260704T120000Z.json`; один протокол теряется или файл может быть прочитан ботом/дайджестом в промежуточном неполном состоянии. M13 закрыт не на этом боевом пути.