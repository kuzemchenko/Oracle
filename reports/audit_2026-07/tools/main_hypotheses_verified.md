# Верификация гипотез главным аудитором (по коду/данным) — 2026-07-14

Дополняет фактологию субагентов (agent1..4). Каждый вывод — со ссылкой.

## H1 (направленность скана) — ОПРОВЕРГНУТО
Скан **event-first**, не instrument-first.
- `orchestrator/event_scan.py:315` — ценовой скан кормится от `U.sealable_universe(con=con)` (динамический брандмауэр: все инструменты с ≥MIN_SEALABLE_BARS в БД, ~241), НЕ хардкод-список из 14.
- `orchestrator/universe_resolver.py:53` `discovery_is_open()` — скан/каскады «думают о любом эффекте в мире».
- Бывший `context.CORE` низведён до `CALIBRATION_SEED` (universe_resolver.py:32) — «НЕ предел открытия».
- Крон 09:00 (`ops/crontab.txt:51`): `run.py --mode event_first --k 2 --seal --vet --deep`.
- Новостной канал открыт (`detect_news_clusters`, не по тикерам); картограф маппит новость→компанию динамически.
- НЮАНС (ограничение, не опровержение): ценовой канал ограничен инструментами, УЖЕ лежащими в БД
  (sealable_universe ~241), а не полным enumeration 100–300 на событие — это Э4/Э5 (см. H7 стрим 4),
  боевой перебор ещё не в проде. Event-first РЕАЛЬНО, но охват инструментов не максимальный.

## H4 (амплитуда) — ЧАСТИЧНО
Боевые ворота всё ещё на БЕЗУСЛОВНОЙ OLS-бете; условный движок построен (Д3), но НЕ подключён.
- `mathlib/cascade.py:58 ols_beta` (безусловная OLS y~x, r2=corr²), `node_sensitivity` (:102) — боевой путь.
- `mathlib/cascade.py:125 conditional_sensitivity` → `mathlib/calibration/conditional.py` (Д3 event-study,
  эпизоды 0.5σ·√окна, walk-forward). НО docstring :130-132 прямо: «какое измерение кормит ворота/ранг —
  решение этапа Э4(в,г), не Д3». → фикс есть, в ворота НЕ включён (ждёт Э4в,г/Э5).
- Порог слабого переноса `WEAK_R2 = 0.10` (:27) — «ниже помечается слабым, но не запрещается».
- (Распределение R²/tier по прогонам — см. agent3.)

## H6 (таймскейл) — ПОДТВЕРЖДЕНО (данные)
5-дневное окно каскада тонет в шуме на ликвидных инструментах ядра.
Скрипт `reports/audit_2026-07/tools/h6_timescale.py` (read-only storage/oracle.db, 130 торговых дней):
- Медианный |5-дн ход| по ядру = **0.70σ шума окна** (<1 ⇒ типовое 5-дн движение НЕ различимо на фоне шума).
- Различимый (>2σ окна) ход лишь в **67/1625 = 4.1%** 5-дневных окон.
- Пример: SPY 5-дн медиана 1.21% при σ_дн 0.88% (0.62σ окна); BNO 5.42% при σ_дн 3.45% (0.70σ).
- Вывод: EVENT_WINDOW_DAYS=5 (cascade.py:44) структурно не ловит многомесячные феномены (класс Tesla/Nvidia)
  → аргумент под медленный трек П2 (горизонты месяцы, отдельный FDR-фон).

## H7 (статус четырёх утверждённых стримов) — с доказательством
Стримы = программа «Поисковый движок» (ROADMAP_2026-07_search_engine.md, подписана 13.07):
1. **Фикс FDR-регрессии** → Д1-Вариант2 (СЕССИЯ 14.07): FDR понижен в ярлык честности, идеи к суду по
   «заметным аномалиям». Код влит (коммиты 091f82b, b6699d0), гейт stage-review в процессе, тег se-d1v2
   ещё не поставлен. СТАТУС: по сути СДЕЛАН, формально в гейте.
2. **event-first скан** → СДЕЛАН давно (event_scan.py, крон event_first). СТАТУС: в проде.
3. **условный event-study движок амплитуды** → Д3 (тег `se-d3`): `mathlib/calibration/conditional.py`,
   `cascade.conditional_sensitivity`. СТАТУС: ПОСТРОЕН, но в ворота НЕ включён (H4; ждёт Э4в,г).
4. **enumeration 100–300 инструментов на событие** → Э4-каркас (тег `se-e4`): `orchestrator/world_enum.py`.
   СТАТУС: КАРКАС СДЕЛАН; боевой запуск (Э5) НЕ НАЧАТ (последний открытый этап программы).
Теги подтверждают: `se-d2, se-d3, se-e4` (+base). se-d1 (исходная df-гипотеза) НЕ поставлен — гейт не прошёл 13.07.

## §8 / §11 — опоры для предложений
- §8 (spec/MASTER_SPEC.md:154) — 13 обязательных полей отчёта → каркас блоков «Разбора дня» (П3б).
- §11 Gate Б→Д (spec/MASTER_SPEC.md:193): 270+ разрешённых прогнозов (значимость hit ~55% vs 50%)
  ИЛИ письменная подпись владельца; калибровка ±10 п.п.; превышение бенчмарка на бумаге. НЕ смягчается.

## Ops-инцидент (побочно, требует внимания владельца)
- `journal/bot.log` 14.07 09:45–11:12: многократный `OSError(28, 'No space left on device')` — бот
  ошибался на записи. Сейчас `df -h /` = 11G свободно (70%), т.е. переполнение отпустило. Крупнейший
  съедатель — `.claude/worktrees/` 703M (9 воркдеревьев ревью, одно раздуто до 542M). Не блокирует
  аудит, но объясняет часть сбоев доставки; хвосты воркдеревьев ревью стоит подчистить (вне этой сессии).
