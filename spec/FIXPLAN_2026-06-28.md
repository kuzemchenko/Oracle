# Единый план фиксов — Оракул, 28.06.2026

Свод двух мультиагентских ревью: **flow-анализ** (`wf_777106af`, 30/39 подтв.) + **конкурентное код-ревью всего репо** (`wf_8970337a`, 135/161 подтв. → ~70 уникальных дефектов). Дедуплицировано, проранжировано по влиянию на «рекомендации потекли» + инварианты. Кросс-ссылки: `A#` = flow-находка, `§N.M` = код-ревью.

## Диагноз (оба ревью согласны)
Скан и генерация идей ЗДОРОВЫ (картограф ~3.84 идеи/прогон, дайджест 6–8 карточек реально доставляется). Обнуляется **финал воронки: суд → seal → выдача**. Самый острый системный класс — **рассинхрон контрактов между модулями** при перестройке на cascade-first (один дефект всплыл ~15 раз): слепой судья и владелец систематически получают обеднённое/ложное состояние → «нет идей» и проигрыши суда часто от дыр контракта, а не по существу.

---

## P0 — БЛОКЕРЫ: оживить money-путь + защитить инварианты (чиню кодом, без согласований)

1. **Голодный вход судьи — контракт полей** (A3 = §3.1). `node_to_facts` отдаёт `edge_total/reliability/lag_days` и теряет `провенанс_звеньев`, а `_money_thesis`/`дело_каскада` читают старые имена `amplitude_total/reliability_r2/lag_total/провенанс_звеньев` → судья видит `full=None, r2=None, lag=None, механизм=[]` → честно валит «нет данных». **Это корень «суд валит 100%».** Фикс: читать актуальные имена + прокинуть `провенанс_звеньев` в facts. Файлы: `event_first.py:224-249,343-352`, `graph_build.py:74-96`.
2. **Инвертированный money-гейт** (A1 = §1.1/§1.2). Судится только `money[:3]`, seal-цикл идёт по ВСЕМУ треку; `_money_kind(None)='cascade_money'` → несуженный хвост запечатывается в §11 БЕЗ суда, а суженные ломаются. `except: out[sym]=None` → fail-OPEN. Фикс: `_money_kind` без валидного вердикта → `cascade_provisional`; маркер `ОШИБКА_СУДА`; `--seal` требует `--vet`; судить весь money-трек (суд ~$1/идея). Файлы: `event_first.py:200-201,379-380,491-500`, `run.py:168-170`.
3. **seal_spec без ярус-гейта (defense-in-depth)** (§1.8). `cascade_money` штампуется только при `research==False` и all-A; иначе принудительно provisional. Файл: `cascade_resolve.py:101-133`.
4. **Антиманип-вето МЁРТВО** (A6 = §1.10). `thresholds.yaml:226 score_block_threshold:7.0` (0–10) vs код читает `балл_порог`→дефолт **70** → недостижимо. **Затрагивает мой код в.3** (`_deep_report_money` `manip_thr=балл_порог||70`). Фикс: единое имя ключа + дефолт 7, общая функция чтения для funnel+event_first. Файлы: `thresholds.yaml:226`, `event_first.py:262-263`, `funnel.py:235`.
5. **Форвард-промоушен пропускает base-rate-монетку** (§1.4/§2.6 — МОЙ новый код). Биномтест против фикс. p=0.5 и `Brier<0.25` без поправки на базовую ставку → нулевой условный скилл промоутится в money (climatology Brier при base-rate≠0.5 всегда <0.25). + `gain or 1.0` фабрикует амплитуду; + перекрытие горизонтов трактуется как независимое; + нет FDR. Фикс: тест против эмпирической base-rate, Brier skill score vs climatology, требовать edge НАД базой; не промоутить при неизвестной величине; эффективный N. Файлы: `mathlib/calibration/forward_promotion.py:27,43,55-90`, `cascade_build.py:88-97`.
6. **Калибровочная baseline протекает в §11** (§1.3/§1.12). `PROVISIONAL_KINDS` исключает только провизорные; `kind='calibration'` (P=0.5 монетка) идёт в `money_out`+гейт-270 (96/96 текущих исходов = calibration, до_ворот=174 на baseline). Два рассогласованных счётчика (resolve герметичен, calibrate — нет). Фикс: алоулист `MONEY_EDGE_KINDS={funnel_forward,theme_daily,cascade_money}`; единый источник гейта-270. Файлы: `resolve.py:32,120,136`, `calibrate.py:114-117,148`.
7. **probability=None засчитывается в гейт-270, но не в Brier** (§1.5). Судья при УСТОЯЛА может не дать probability; seal проходит (нет в REQUIRED_FIELDS) → гейт «созревает» на неизмеримых. Фикс: невалидная probability → не-УСТОЯЛА И/ИЛИ probability в REQUIRED_FIELDS для форвард-kind; гейт-270 по тому же подмножеству, что Brier. Файлы: `debate.py:277,286`, `forecast.py:72,84`, `resolve.py:113-118`.
8. **Сырой close вместо adjusted_close по всему каскаду** (§5.1). `cascade_build/graph_build/event_first._window_return` читают `close`; 85% строк имеют adj≠close → на сплите/дивиденде ложный скачок шока/edge/σ. Фикс: везде `.adj`/loader. Файлы: `cascade_build.py:200-213`, `graph_build.py:32-53`, `event_first.py:56-62`.
9. **Бюджетный гард event_first отсутствует** (§0.1, КРИТ). Боевой `--vet --deep` делает live LLM-вызовы без `RB.precheck`+`cost_guard` (в отличие от funnel/masked); режима нет в limits.yaml. Нарушает Инв#5/§24. Фикс: добавить `event_first` в обе таблицы limits.yaml; обернуть live-путь. Файлы: `limits.yaml:37-55`, `event_first.py:319,357,373`.

## P1 — ВЫДАЧА ДОХОДИТ ДО ПОЛЬЗОВАТЕЛЯ (чиню кодом)

10. **Бот заземляется на мёртвую схему** (§6.1/§6.2). `context_brief` фильтрует `mode=='live'` (крон=`auto` → «боевых нет»); `ideas_from_protocol` читает легаси `этап6_синтез` → [] при 5 выданных идеях → Дирижёр говорит «идей нет». Фикс: боевыми считать `auto+live`; для ef-протоколов брать идеи из `картограф_идеи`/`граф_отбор.топ_k`. Файлы: `bot_chat.py:117-134`, `bot_reports.py:92-95`.
11. **Нет ветки промоушена идеи в actionable §8-карточку** (A2 = §6.5). Money-каскад, устоявший в суде, должен идти отдельной §8-карточкой (через существующий `format_report`+кнопка §12), не только в research-дайджест; `ef_` сейчас делает `continue` до неё. Решить вместе с фреймингом (C2: метку «не рекомендация» НЕ снимать до калибр-гейта §11). Файлы: `bot.py:712-740`.
12. **Самопротиворечие дайджеста** (A4). «💰 N можно довести до ставки» берёт money ДО суда; в том же сообщении они РАЗБИТА. Фикс: считать после `суд_money` (исключать РАЗБИТА). Файл: `bot_reports.py:821`.
13. **Рудимент «контур выдал 0 идей»** (A5 = §6 косвенно). top3≡0 под `--vet` (поле не читается доставкой, но кормит ВОСПРИЯТИЕ «ноль» в cron.log/`/progress`). Фикс: строить top3 из `граф_отбор.топ_k` ЛИБО заменить метрику на «FDR→узлы→идеи дайджеста». Файлы: `event_first.py:413,510,558`, `run.py:188`.
14. **Дайджест режет картограф `[:3]`** (C3). При 6–7 идеях половина не показывается. Фикс: показывать все / «ещё N — /progress». Файл: `bot_reports.py:810`.
15. **«разбери ТИКЕР» рекламируется, но не подключено** (§6.5). Интент матчит только «идею»; `разбери CLF`→обычный чат (одно семейство), не состязательный разбор. Файлы: `bot.py:237-245`.

## P2 — ЧЕСТНОСТЬ ЧИСЕЛ И ЗАПЕЧАТЫВАНИЯ (часть требует согласия по порогам)

16. **Walk-forward — ложная устойчивость пина** (§2.1). Берётся только `fd.train`, `fd.test` игнорируется, окна перекрыты на 75% → `rel_dispersion` занижен → пин слишком легко; последний год не в train. Фикс: непересекающиеся окна (step≥train) или OOS на `fd.test`. Файл: `sensitivity.py:19-57`.
17. **base_rate из LLM** (§2.2). `base_rate=gen_j.get(...)` — выдуманное число, якорь судье + в запечатанный прогноз. Есть детерминированный `_empirical_base_rate()`, не подключён. Нарушает Инв#6. Фикс: считать в mathlib, подавать судье/прогнозу. Файлы: `debate.py:119,148,184`.
18. **Изоляционный R² на дато-невыровненных рядах** (§2.3). Позиционный `zip` двух `_closes` → мусорный R² (триггер живой: SPCX/RKLB). Фикс: `load_aligned([root,term])`. Файл: `graph_build.py:31-54`.
19. **p-value объёмного z как нормальная** (§2.4). `erfc(|z|/√2)` для тяжелохвостого объёма → почти всё «проходит FDR». Фикс: лог/эмпирический фон для объёма; t-df для ретёрнов. Файлы: `event_scan.py:63-73`.
20. **calibration_band без минимума N на корзину** (§1.7). `max(gaps)` при n≥1: одна точка даёт 95 п.п. → ложный KILL/§11. Фикс: игнорировать корзины n<5–10 / усадка. Файл: `brier.py:53-58`.
21. **Журнал не защищён от удаления/перестановки + resolve не верифицирует печать** (§8.1/§8.2). Нет hash-chain; `verify_seal`/`verify_all` существуют, но не вызываются перед резолвом. Фикс: hash-chain (prev_hash) + `verify_seal` перед вынесением исхода. Файлы: `sealing.py:65-121`, `resolve.py:66-92`.
22. **limits.yaml gates/KILL не читается кодом** (§1.11). 270/band/kill_* захардкожены/без потребителя → KILL программно не проверяется (только LLM-скилл, напряжение с Инв#6). Фикс: читать gates из конфига + детерминированная проверка KILL. Файлы: `limits.yaml:57-63`, `resolve.py:136`.

## P3 — СОСТЯЗАТЕЛЬНОСТЬ П10 + ПОРТФЕЛЬ (чиню кодом)

23. **Развязка семейств держится на конфиге/случайности** (§4.1–4.4). `_generator_family()` из конфига, не из реальной модели; `generator.fallbacks` содержит `openai/gpt-5.5`==`critic.primary`; судья развязан только с генератором, не с критиком; роли ревьюера протекают судье. Фикс: `family_of(модель)` пост-вызова; симметричный constraint generator↔critic↔judge с RuntimeError при невозможности; ревьюеру — обезличенные A/B/C. Файлы: `debate.py:73-75,133,140-142,174-185`, `models.yaml:23,142-145`.
24. **MACRO_DRIVER устарел → unmapped склеивается в одну ставку** (§7.1). Весь cascade-универсум `unmapped`, но `correlation_map` трактует сентинел как драйвер → «одна ставка» вместо N. Фикс: не кластеризовать unmapped; дополнить драйверы с источником. Файл: `portfolio.py:28-85`.
25. **Шок не привязан ко времени события** (§5.3 = частично P0#8). Окно шока = последние N баров без asof. Фикс: окно ОТ даты события + asof-гейт.

## СЛОЙ Б — РЕШЕНИЯ ВЛАДЕЛЬЦА (пороги/рубрика, меняю только с согласия)
- **B1 каскадная под-рубрика** (§debate rubric): порог 3.0 ок, но рубрика требует «кто продаёт/отыграно/позиционирование» — данных, которых каскадный узел не несёт. Версионированная под-рубрика для каскадов ЛИБО обогащать cand (после P0#1). Связано с П10.
- **B2** отвязать cap источников от k (§event_first:84). **B3** дедуп near-дубль кластеров. **B4** ускорить накопление форвард-промоушенов (узкое место яруса A). **B5 НЕ трогать FDR q=0.1** — это терреин, лечится вселенной.
- **Фрейминг C2:** метку «исследование, не рекомендация» снимать для money ТОЛЬКО после прохождения денежного гейта §11 (мандат §1/§8/§11/П16).

## ГИГИЕНА / СЕКРЕТЫ
- **9.3** plaintext-токен стороннего femmecode-бота в committed `crontab.txt:61-66` (+ чужие задачи) → вынести, ротировать.
- Мёртвые конфиги/код: `cascade_sensitivities.yaml` HEADER лжёт «вход движка» (§9.1); `build_graph` test-only (§3.2); dryrun рассинхрон (§3.3); devops-петля mock+live + graduation без Brier-гейта (§9.2); `_sealed_cascades_for`/`_pro_con` мёртвые схемы (§6.6).
- Длинный хвост low (§2.7,5.6,6.6,7.8,8.6) — гигиена по мере касания файлов.

## Порядок исполнения
P0 (особенно #1 контракт полей, #2 гейт, #5 мой промоушен-баг) → перепрогон event_first → проверить, что суд перестаёт валить 100% и идеи доходят → P1 (выдача) → P2/P3. Слой Б — отдельным заходом с подписью владельца.
