# Кросс-вендорное ревью 20260704T142143Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П2а, ПОДТВЕРЖДАЮЩИЙ прогон №2. Твои находки №1 закрыты: (BLOCKER) assign_key под fcntl-локом (проверка+append атомарны); (HIGH) реестр ВЫШЕ сидов + первое использование сида журналируется (правка yaml не пересдаёт); (HIGH) инъекция registry-dict удалена из контракта — только registry_path; (HIGH) битая строка реестра пропускается с пометкой; (LOW) set сортируется; (LOW) тест-тавтология заменена. Плюс fix_keys=False для mock (журнал не трогается). Подтверди закрытие и скажи, есть ли НОВЫЕ BLOCKER/HIGH с контрпримером на боевом пути. LOW — в бэклог, не повторяй известное.
Файлы: orchestrator/attention_field.py, orchestrator/tests/test_attention_field.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/attention_field.py:65 — `_load_registry()` не валидирует, что JSON-запись реестра является объектом; валидная JSON-строка/массив/`null` валит боевой путь через `rec.get(...)`, хотя контракт говорит, что битая строка реестра пропускается с пометкой. Контрпример: в `journal/attention_keys.jsonl` попала строка `null\n` или `[]\n` после обрыва/ручной правки; вызов `field_for_asset(...)` → `_load_registry()` → `rec.get("актив")` → `AttributeError`, весь прогон падает вместо честного `не_измерено`/пропуска строки.

- [HIGH] orchestrator/attention_field.py:65-66,114-117 — malformed object-запись с тем же активом, но без валидного `ключ`, считается реестровой записью и блокирует последующую валидную запись из-за `setdefault`; при наличии seed ещё и позволяет посчитать поле по seed без журналирования первого фактического использования. Контрпример: реестр:
  ```jsonl
  {"актив":"BNO.US"}
  {"актив":"BNO.US","ключ":"brent oil","источник":"ключи новостного кластера картографа","run_id":"r1","ts":"2026-07-04T09:00:00+00:00"}
  ```
  `_load_registry()` сохранит первую запись, вторую проигнорирует. Далее:
  - без seed `resolve_key()` вернёт `(None, None)` несмотря на валидную запись ниже;
  - с `seeds={"BNO.US":"new seed"}` `field_for_asset()` возьмёт seed, но условие `asset not in registry` ложно, поэтому `assign_key()` не вызовется и фактически используемый ключ не будет зафиксирован в append-only журнале. Это нарушает запрет пересдачи/провенанс ключа на боевом пути.