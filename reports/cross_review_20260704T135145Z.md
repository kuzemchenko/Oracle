# Кросс-вендорное ревью 20260704T135145Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П1-гейт, ПОДТВЕРЖДАЮЩИЙ прогон №2 (первый: НЕ ПРОЙДЕН — 1 BLOCKER timeframe-подмена + 3 HIGH). Всё закрыто: timeframe теперь колонка trends (идемпотентная миграция в news_common.db_connect._migrate), rows_for_attention фильтрует канон+последний фетч; argmax → последнее касание максимума (плато→ЛОВУШКА); единый контракт выхода (_empty_result); провенанс фетч_utc + asof/max_age_days → честное устаревание (нечитаемые метки — видимая пометка); устойчивость хайпа = ≥2 СМЕЖНЫХ точек у пика (два изолированных спайка — не хайп). Проверь, закрыты ли твои находки №1, и нет ли НОВЫХ блокеров. LOW-бэклог уже зафиксирован в goals.md (вторая волна, обрыв на LEVEL_COLD, recent под дневную сетку) — не повторяй его.
Файлы: mathlib/attention.py, mathlib/tests/test_attention.py, data/trends.py, data/news_common.py, ops/tests/test_trends_rows.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] data/news_common.py:342 — миграция `ALTER TABLE trends ADD COLUMN timeframe TEXT DEFAULT 'today 3-m'` нечестно маркирует все легаси-строки как канонические — контрпример: старая БД до миграции содержит строки, полученные командой `data/trends.py --keyword uranium --timeframe "today 12-m"` или старым конфигом не `today 3-m`; после `_migrate()` все они получают `timeframe='today 3-m'`, `data/trends.py:152` (`rows_for_attention`) выбирает их как канонические, и `attention_score` применяет пороги 3‑месячного окна к 12‑месячной нормировке. Это фактическое сохранение прежней timeframe-подмены для восстановленных/легаси БД. Честный вариант — `NULL/unknown` для старых строк и не использовать их в каноническом расчёте без перефетча.

- [HIGH] mathlib/attention.py:205 — staleness-защита обходится при отсутствующем `fetched_at`: если строки 2–3-элементные или в БД `fetched_at IS NULL`, `last_fetch=None`, проверка `asof/max_age_days` вообще не выполняется, но возвращается числовой `score` — контрпример: `attention_from_rows([("2020-01-01",10,0), ... 9 точек ...], asof="2026-07-04T09:00:00+00:00", max_age_days=7)` выдаст фазу/score по заведомо старому ряду с `фетч_utc=None`, вместо честного `score=None` / «нет метки свежести».

- [HIGH] mathlib/attention.py:211 — проверка устаревания падает на смешении naive/aware ISO-меток, перехватывается только `ValueError` — контрпример: `fetched_at="2026-06-01T00:00:00"` и `asof="2026-07-04T09:00:00+00:00"` успешно парсятся, но `(a_dt - f_dt)` бросает `TypeError: can't subtract offset-naive and offset-aware datetimes`; вместо видимой пометки/`score=None` потребитель получает исключение.