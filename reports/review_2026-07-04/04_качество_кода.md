# Отчёт код-ревью /root/oracle (качество кода и корректность) — 2026-07-04

## 0. Фактический прогон тестов

`pytest mathlib orchestrator` в .venv: **407 passed, 2 failed за 13.3s**. Оба падения — известный env-долг `test_smoke_funnel.py` (theme-guard NVDA.US: `resolve_theme("NVDA.US")` возвращает `('NVDA.US','dynamic')` вместо `(None,None)`, и guard не отказывает). **Новых падений нет.**

Статический проход: ruff/pyflakes/flake8 отсутствуют — заменён на `python -m compileall` (все модули компилируются чисто) + целевые grep-проходы (naive datetime, except-pass, mutable defaults, json.loads, блокировки файлов).

## 1. Статус известных багов (проверено на текущем коде)

- **debate.py:215 — НЕ исправлен.** В возвращаемый протокол пишется `"направление": direction` (исходное у кандидата), а не `eff_direction`, выведенный генератором (стр. 143) — им пользовались критик/судья/base_rate. Для masked-кейсов протокол показывает `направление: None`.
- **portfolio.py:174 — НЕ исправлен.** `lim.check_monthly_risk(0.0, total, ...)` — уже потраченный месячный риск захардкожен 0.0; проверяется только текущая корзина, месячный лимит §11 фактически не работает.

## 2. HIGH (новые)

**H1. Дашборд и абляция читают исходы из журнала, где их нет — оба механизма наблюдаемости мертвы.**
- `dashboard/build_dashboard.py:64-69, 86-88` и `orchestrator/ablation.py:146-164` берут `pred.get("observed_value")/("observed_at")` из `journal/predictions.jsonl`. Этот журнал append-only и исходов не содержит (проверено: 0 вхождений на 409 записей); исходы пишет `resolve.py` в отдельный `journal/outcomes.jsonl` (154 записи).
- Отказ: калибровка/hit-rate на дашборде вечно «накапливается», а часть 2 абляции («вклад агента в Brier» — петля качества §25) никогда не наступит, при реально идущей сверке.
- Фикс: join с `outcomes.jsonl` по `hash` (паттерн уже есть в resolve.py/calibrate).

**H2. Крах дашборда, когда H1 починят или исходов мало.** `build_dashboard.py:80`: `round(B.calibration_band_pp(...), 2)`, а `calibration_band_pp` возвращает **None**, если ни одна корзина не набрала MIN_BIN_N=5 (brier.py) → `TypeError: round(None)`, падает весь `main()`. Нужна None-ветка.

**H3. П8-ворота masked-контура всегда «чисто».** `masked.py:160-170` `_p8_violations_total` суммирует только `isinstance(v, int)`, а `p8_violations` в записях ролей — **список** строк (agents.py:64 ← `judgment.validate_p8` возвращает list) → сумма всегда 0, `p8_clean=True` у любого кейса. Gate §23.2(б) слеп к фабрикации оснований. Фикс: `len(v) if isinstance(v, list) else ...`.

**H4. KeyError-крэш на бюджетном стопе masked-прогона.** `run.py:30-47` обрабатывает только `"ОТКАЗ_бюджет"`, но `masked.run_masked` при стопе на лету возвращает `"ОСТАНОВ_бюджет"` (masked.py:267) без ключа `агрегат` → `s["агрегат"]` падает трейсбеком в cron. Для funnel-пути run.py:180 оба ключа уже обрабатывает — паттерн известен, но сюда не донесён.

**H5. Гонка на journal/predictions.jsonl рвёт hash-цепь; прогноз навсегда выпадает из сверки.** `mathlib/sealing.py:82,100-104`: `seal()` = «перечитать весь журнал → prev_hash последней записи → append» без блокировки. Конкурентные писатели существуют (cron `event_first --seal`, `calibrate`, ручной funnel live). Два одновременных seal → одинаковый prev_hash → `verify_all` метит вторую запись битой → `resolve.py:88-91` навсегда отказывается выносить по ней исход (ошибка повторяется каждый прогон). Бонус: `_last_hash` даёт O(n²) рост. Фикс: `fcntl.flock` вокруг read-last+append.

**H6. Кривая ФОРМА LLM-ответа валит весь money-прогон.** `judgment.py:120-123` (`parse`): элементы `кандидаты` считаются dict без проверки — LLM вернула список строк → `AttributeError`, который не является `JudgmentError` и не ловится ни agents.py:57, ни `run_funnel` (funnel.py:417 ловит только `RunBudgetExceeded`). Тот же паттерн — `debate.py:287` `_rubric_mean` (`o.get("балл")` по невалидированным элементам `оценки`). Фикс: `isinstance`-проверки → `JudgmentError`.

**H7. Суд идеи по не-CORE активу идёт без рыночных данных.** `challenge.py:196` + `context.py:299-311`: `build_context` грузит котировки/индикаторы только для CORE (6 ETF), а разбор идёт в основном по компаниям каскада → в деле судьи `котировка=None, индикаторы=None, base_rate=None`, хотя история в oracle.db есть. Судья систематически штрафует «нет данных» на пустом месте — бьёт по основному продуктовому потоку.

## 3. MEDIUM

**M1. Fail-open вето на money-пути.** `event_first.py:302-316` `_deep_report_money`: `except Exception → quality[key]=None` → «вето нет», идея запечатывается в §11-трек без обещанной `--deep` проверки. Противоречит fail-closed философии F0#2 этого же файла. Минимум — демотировать или явно помечать «проверка не выполнена».

**M2. Бюджетный ОСТАНОВ журналируется как «тема вне универсума».** `funnel.py:421-433` + `619-643`: `_write_refusal` различает только `ОТКАЗ_бюджет`/else→`ОТКАЗ_тема`; стоп по бюджету пишет md «ОТКАЗ прогона: тема вне универсума» с reason=None. Ложная запись на money-пути §24.

**M3. Бюджет-стоп внутренней воронки в event_first глотается.** `event_first.py:479-493`: `run_funnel` сам ловит `RunBudgetExceeded` → event_first видит обычный dict, продолжает тратить; guard event_first не видит трат внутренних funnel → реальный потолок ≈ k×cap(funnel_full)+cap(event_first). Плюс funnel.py:423-424 зовёт `PROG.finish` на чужом прогрессе — внешний прогон помечается «завершён».

**M4. Конкурентный `run_resolve` даёт дубли исходов.** `resolve.py:69-120`: `done` в памяти + append без lock + нет дедупа по hash при чтении → параллельные запуски (cron + ручной) навсегда искажают Brier/калибровку и приближают ворота-270.

**M5. Дубль скана с уже отвергнутой статистикой.** `funnel.py:171-205` `stage1_scan` — параллельная реализация `event_scan.price_vol_signals`, но p-value по нормали (`erfc`), тогда как F2#19 перевёл боевой скан на Student-t именно из-за занижения p → FDR в funnel-пути переоткрывает шум. Одну реализацию удалить.

**M6. Ретраев в OpenRouter-клиенте нет вообще.** `openrouter.py:199-218`: 429/5xx → немедленная смена модели без паузы; комментарий в masked.py:307 («у клиента есть ретраи») ложен. Короткий rate-limit выжигает всю цепочку → ложные `ok=False`, особенно в параллельных masked-кейсах.

**M7. Вызовы с `cost=None` невидимы для бюджета.** `openrouter.py:206-207` + `run_budget.py:166-170`: `RunBudgetGuard.add(None)` молча пропускает → стоп-на-лету и месячные суммы систематически занижены (Инв#5 — тихая дыра). Фикс: оценка по прайс-листу или счётчик неучтённых вызовов.

**M8. `context.py:247` — параметр `asof` принимается и игнорируется** → вызывающий с replay-намерением получает look-ahead (риск П16). Лучше `NotImplementedError`.

**M9. Смешение форматов ключа хронологии.** `challenge.py:48-57`: `_ts_key` возвращает то ISO `2026-07-04T…`, то компакт `20260704T…`; лексикографически компакт всегда «новее» → `find_idea/list_ideas` берут не ту идею.

**M10. Рассинхронизация ряда и partial-флагов.** `mathlib/attention.py:70-71`: `vals` фильтруется от None, `is_partial` — нет; `_trim_partial` попарит списки разной длины → флаги съезжают → фейковое «остывание»/ЛОВУШКА.

**M11. `mathlib/cascade.py:354`: `amp - float(realized_move)` при `realized_move=None`** (штатный `window_return` возвращает None на короткой истории) → TypeError вместо «нет данных».

**M12. Несогласованные словари направлений.** `mathlib/portfolio.py:79-80`: `_sign` даёт «+» только точной строке «лонг»; `"long"/"buy"/None` → «-» (шорт), при том что `base_rate._dir_sign` синонимы понимает → знак нетто-экспозиции и карта корреляций неверны. Смежное: `forecast.py:33-40` `direction_to_side` — подстрочный матчинг («не лонг» → above) уходит в запечатанный прогноз.

**M13. Неатомарная запись протоколов + run_id с точностью до секунды.** `funnel._write_protocol/_write_refusal`, `event_first.py:673-675`, `multi_event._write`: `open("w")+json.dump` — обрыв оставляет битый JSON (ablation его молча скипает, бот/дашборд падают); два прогона в одну секунду перезапишут протоколы друг друга. Правильный паттерн (tmp+os.replace) уже есть в `progress._write`.

**M14. Счётчик «демотировано_судом» лжёт.** `event_first.py:558-568`: при `--seal` без `--vet` инкрементируется, хотя суд не запускался.

**M15. XSS/неэкранированные подстановки в дашборде.** `build_dashboard.py:316-317, 342-344` — значения из `journal/funnel_logs/*.json` идут в HTML без `_h()`. Плюс `:107` — `null` в логе воронки → TypeError в sum.

**M16. Дебаты без критика легитимны.** `debate.py:151-212`: если модели критика исчерпаны (`ok=False`), в дело кладётся заглушка и «УСТОЯЛА» выносится без ред-тима — состязательность (Инв#2) выключена без пометки/деградации.

## 4. LOW (сжато)

- **Таймзоны:** `progress.py:247` — `time.mktime(strptime("...Z"))` трактует UTC-строку как локальное время → stale-детект сдвинут на пояс (нужен `calendar.timegm`); `event_mapping.py:28`, `quant.py:32`, `ops/budget.py:173` — `date.today()` (локаль) при UTC-системе; `resolve.py:65` — захардкожено `T20:00:00+00:00` как закрытие US-сессии (зимой 21:00 UTC); `mathlib/outcomes.py:38` — лексикографическое сравнение времён ломается при смешении `Z` и `+00:00` форматов (оба существуют в проекте).
- **Глотание ошибок:** `quant.py:39-40` except→pass на подкачке EODHD (данные для money-решений теряются молча); `context.py:334-339` — тектоника глотается без записи в `data_gaps`; девять `pass` в `progress.py` — осознанный best-effort, ок.
- `mathlib/behavioral.py:87` — `if prices` на np.ndarray → «truth value ambiguous»; `mathlib/tectonic.py:109` — `far["order"] >= 3` при order=None → TypeError; `mathlib/waves.py:53-54` — деление на `p[0]`/`ext_price` без проверки нуля; `mathlib/graph_select.py:57` — `symbol=None` → фиктивные коллизии "NONE".
- `universe_resolver.py` — подсчёт баров без `close IS NOT NULL`; `cascade_resolve.py:64` — connect создаёт пустой .db и падает OperationalError; `challenge.py:142-144` — подстрочный поиск актива (`"SO" in "USO.US"`).
- **Мёртвый код/дубли:** `event_first._last_return` не вызывается; блок шок-источников в dryrun — копия `_shock_sources`; `openrouter.py` — мёртвый конфиг `reasoning`, нет `max_tokens` (стоимость вызова не ограничена), мёртвый параметр `models` в `family_of`; dashboard дважды читает/resolve-ит весь журнал; `judgment.py:40` fence-регэксп берёт первый ```-блок; `synthesis.py:31-47` `_parse_horizon_text` возвращает первый срок вместо самого длинного и матчит «дн» в подстроках.
- **Магические константы → config/:** `run.py:178` vet_money_k=3; `event_first` MAX_SHOCK_SOURCES=8/top_k=8/horizon_days=5; `quant._timing` пороги 0.5/1.5σ дублируют `config/thresholds.yaml:timing` (два разных определения РАНО/ПОЗДНО); funnel stage6 capital_anchor 100000 и b=1.0; `mathlib/options.py:108` OI>1000; `event_mapping.MIN_VOL=100000`; cascade_build gain_sd=|β|×0.5, R2_PIN_CAP=0.95.
- **Структура:** funnel.py 805 строк — `_run_funnel` ~180 строк и `_render_md` ~150 строк (рендер вынести по образцу masked.render_md). `mathlib/portfolio.py:60-70` — вечный кэш `_DRIVER_MAP` в долгоживущем боте не подхватит правку universe.yaml; :114 — предупреждение о сырьевой концентрации только для лонгов.

## 5. Проверено и чисто / не подтвердилось

- Чисто: `agents.py`, `registry.py`, `build_prompts.py`, `fdr.py`, `brier.py`, `tailprob.py`, `kelly.py`, `masked_eval.py`, `indicators.py`, `base_rate.py`.
- Не подтвердились (ложные тревоги отсеяны): проглатывание `RunBudgetExceeded` в masked-пуле (BaseException корректно пролетает), инверсия вероятности при `below` в cascade_resolve, fail-closed `_money_kind` для несуженных узлов — работают верно.
- Известные из CLAUDE.md, статус подтверждён: `cascade.py:217` лаг направленно-агностичен; hash-chain не ловит усечение хвоста журнала.

## 6. Приоритет починки

1) H1+H2 (дашборд/абляция слепы к исходам — петля качества §25 не наступит никогда) и H3 (мёртвые П8-ворота) — тихо отключённые контрольные механизмы; 2) H5+M4 (гонки на журналах — необратимая порча hash-цепи и Brier); 3) H4+H6 (крэши money-пути от бюджетного стопа и кривого LLM-ответа); 4) H7 (слепой суд не-CORE активов); 5) известные debate.py:215 и portfolio.py:174.
