# Кросс-вендорное ревью 20260704T155722Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №15 (финальный по challenge.py). Ключ времени: единая каноническая форма strftime(%Y%m%dT%H%M%S) в UTC для ISO и компакта, календарная strptime-валидация, мусор/голая дата → пусто — всё подтверждено исполнением (6 кейсов). ОСТАНОВ и ОТКАЗ бюджета журналируются no-clobber хелпером. За 14 прогонов: 2 BLOCKER + 20 HIGH. Финальный вердикт.
Файлы: orchestrator/challenge.py, orchestrator/tests/test_challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:39,83 — компактное время в коде всё ещё Z-зависимое, хотя заявлена каноническая форма `strftime("%Y%m%dT%H%M%S")` без `Z` — контрпример: протокол `{"run_id": "funnel_20260705T120000__real"}` без `ts` получает `_ts_key == ""`, потому что `_TS_RE = r"\d{8}T\d{6}Z"` требует `Z`; рядом фикстура `{"run_id": "week7_testday"}` тоже получает `""`, и сортировка по `run_id` в reversed-поиске может выбрать фикстуру вместо реального прогона.

- [HIGH] orchestrator/challenge.py:39,314 — новые `run_id` продолжают генерироваться в неканонической компактной форме с `Z`: `_now_compact()` возвращает, например, `20260704T090002Z`, тогда как заявленная единая форма ключа — `20260704T090002`; это сохраняет смешение форматов, из-за которого уже исправлялась хронологическая ошибка ISO/compact.

- [HIGH] orchestrator/challenge.py:337,354 — журналирование бюджетного `ОТКАЗ`/`ОСТАНОВ` зависит от `write`; при `run_challenge(..., mode="live", write=False)` бюджетный отказ `RB.precheck(... allowed=False)` или исключение `RunBudgetExceeded` возвращаются без записи через `_write_challenge_file`, что противоречит требованию «ОСТАНОВ и ОТКАЗ бюджета журналируются no-clobber хелпером».