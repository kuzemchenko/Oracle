# Кросс-вендорное ревью 20260704T154522Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №9. Закрыто из №8: (1) _norm_utc_digits валидирует время (мусор → пустой ключ, фикстуры ниже боевых); (2) котировка и индикаторы инъектируются независимо; (3) резюме утверждает П10 только по развязка_семейств_П10.судья_вне_дебатёров, иначе явный ⚠; (4) bool исключён из вероятности. За 8 прогонов закрыто 2 BLOCKER + 14 HIGH. Финальный вердикт по challenge.py; калибровка: HIGH требует реального ущерба на боевом пути этого проекта (один сервер, один бот).
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:438 — `bool` всё ещё проходит как вероятность судьи — контрпример: если `DBT.run_debate()` вернёт `{"вердикт": {"исход": "УСТОЯЛА", "вероятность_судьи": True}}`, `summarize()` напишет `вероятность по судье ~100%`. Это прямое нарушение закрытого пункта №8(4): `bool` должен быть исключён из вероятности, иначе владелец получает фальшивую числовую уверенность.

- [HIGH] orchestrator/challenge.py:55-64 — `_norm_utc_digits()` принимает дату без времени как валидный хронологический ключ, хотя контракт говорит «НЕ-время → ''» — контрпример: `datetime.fromisoformat("20990101")` валиден и даст ключ `"20990101"`, поэтому тестовый протокол с `{"ts": "20990101", "этап6_синтез": {"отчёты": [...]}}` отсортируется выше всех реальных боевых прогонов, и `find_idea()` без параметров возьмёт фикстуру вместо последней боевой идеи. Это повторяет класс дефекта №8: мусор/фикстура может победить боевые журналы.