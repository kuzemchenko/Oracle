# -*- coding: utf-8 -*-
"""orchestrator — Python-слой «Оракула» (MASTER_SPEC §27).

Дирижёр (в Claude Code — сам Claude; по cron — orchestrator/run.py) не имеет мнения о
рынке: маршрутизация по воронке (§6), сбор поля суждений (§5.2), карта противоречий,
контрфактический протокол абляции (§11.1), процедурное вето. Вызовы чужих семейств моделей
идут через OpenRouter по config/models.yaml с фолбеками (П10).

Подмодули:
  judgment    — стандартное поле суждений Дирижёра §5.2 + валидация П8
  openrouter  — клиент OpenRouter: роли, фолбеки, лог стоимости/смен модели; mock-режим
  context     — сборка рыночного среза из storage/oracle.db + knowledge/ (§5.5 срезы)
  agents      — загрузка промптов agents/prompts/, вызов агента, парсинг суждения
  funnel      — воронка §6: сбор поля суждений всех школ, карта противоречий, протокол
"""
