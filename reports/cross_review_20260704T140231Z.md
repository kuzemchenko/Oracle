# Кросс-вендорное ревью 20260704T140231Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П1-гейт, ПОДТВЕРЖДАЮЩИЙ прогон №5. Закрыто из №4: (1) rows_for_attention отдаёт ВЕСЬ канон окна без SQL-MAX, хронологический выбор последнего фетча — единственным местом в attention_from_rows (naive=UTC, тай-брейк детерминирован); (2) _call пробрасывает устойчивые не-429 сбои как есть, RateLimited только при реальном TooManyRequestsError. Подтверди закрытие №4. НОВЫЕ находки поднимай ТОЛЬКО уровня BLOCKER/HIGH с конкретным контрпримером на БОЕВОМ пути (cron-фетч today 3-m, UTC-метки nc.now_utc_iso фиксированной ширины); теоретические края с нереальными на боевом пути входами — LOW, в бэклог. LOW-бэклог goals.md не повторяй.
Файлы: mathlib/attention.py, mathlib/tests/test_attention.py, data/trends.py, data/news_common.py, ops/tests/test_trends_rows.py

---

ВЕРДИКТ: ПРОЙДЕН

Находки: новых BLOCKER/HIGH на заявленном боевом пути не найдено.

Подтверждение закрытия №4:
- `data/trends.py:rows_for_attention` возвращает весь канонический ряд `timeframe=?` без SQL `MAX(fetched_at)`; выбор последнего фетча перенесён в `mathlib/attention.py:attention_from_rows`.
- `attention_from_rows` выбирает последний `fetched_at` хронологически через ISO parse/timestamp; naive-метки трактуются как UTC; тай-брейк детерминирован через `(timestamp, str(k))`.
- `data/trends.py:_call` больше не маскирует устойчивые не-429 ошибки под `RateLimited`: `RateLimited` поднимается только если последняя устойчивая ошибка — реальный `TooManyRequestsError`; иначе пробрасывается исходное исключение.