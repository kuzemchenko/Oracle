# Кросс-вендорное ревью 20260704T135523Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П1-гейт, ПОДТВЕРЖДАЮЩИЙ прогон №3. Твои находки №2 закрыты: (BLOCKER) миграция даёт легаси-строкам NULL (не канон по умолчанию), боевая БД переведена в NULL, канон наполняется только явной записью store(); (HIGH) требование свежести asof+max_age_days при отсутствии fetched_at → честный score=None «нет метки свежести»; (HIGH) naive/aware метки: naive трактуется как UTC, вычитание не падает, нечитаемые метки — видимая пометка. Проверь закрытие и отсутствие НОВЫХ блокеров. Известный LOW-бэклог (goals.md) не повторяй.
Файлы: mathlib/attention.py, mathlib/tests/test_attention.py, data/trends.py, data/news_common.py, ops/tests/test_trends_rows.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] data/trends.py:82 — `related_queries` с `value="Breakout"` роняет весь `fetch_keyword`, и уже полученный `interest_over_time` не сохраняется — сценарий: Google Trends часто отдаёт rising-запросы со значением `"Breakout"`; `int(r["value"])` вызывает `ValueError`, он не ловится в блоке related, пробрасывается в `run_all`, где ключ помечается ошибкой, а `rows_iot` теряются, хотя комментарий обещает «ряд интереса важнее related — отдаём что есть».

- [HIGH] mathlib/attention.py:246 — выбор последнего фетча делается строковым `max(fetches)`, а не хронологическим сравнением ISO-времени — контрпример: два фетча `"2026-07-04T10:00:00+02:00"` (=08:00 UTC) и `"2026-07-04T09:00:00+00:00"` (=09:00 UTC); строковый `max` выберет первый, более старый фетч, после чего расчёт пойдёт по старой нормировке или даст ложный `score=None` при жёстком `max_age_days`, хотя более свежие строки есть.