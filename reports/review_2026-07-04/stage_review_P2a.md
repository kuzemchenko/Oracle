# Stage-review П2а — протокол (2026-07-04)

Объём: orchestrator/attention_field.py (новый), config/attention_keys.yaml, интеграция
event_first/event_scan, data/trends.py (план фетча), ops/bot_reports.py (рендер) —
REVISION_2026-07 §R4.2 (подписано владельцем 04.07). Цикл: 1 ревью-агент (Claude) +
кросс-вендор openai/gpt-5.5 (6 прогонов).

## Ревью-агент — вердикт НЕ ПРОЙДЕН (по коммиту 02190ec) → все находки закрыты
- Подтверждено: поле строго ПОСЛЕ отбора (annotate → суд), ранжирование/prerank/_money_kind/seal
  поле не читают; §R0#5 None ≠ 1.0 держится; mock/dryrun целы; рендер None-safe; money-карточка
  13 полей не тронута.
- BLOCKER-1 (гонка реестра) и HIGH-1 (сиды выше реестра = скрытая пересдача) — уже были закрыты
  коммитами кросс-цикла (лок + реестр-выше-сидов + журналирование первого касания сида).
- **HIGH-3 (боковой канал, новое):** ключи реестра шли в суточный фетч → event_scan гнал ВСЕ ключи
  через единый FDR → П2а влиял на генерацию/порог отбора будущих прогонов. ЗАКРЫТ: скан читает
  только `trends.scan_keywords()` (конфиг news.yaml); ключи «внимания» фетчатся, но кандидат-события
  не порождают и множественность BH не раздувают.
- **HIGH-4:** кэп роста фетча — MAX_ATTENTION_FETCH_KEYS=30 последних ключей реестра (append-only
  реестр не раздувает фетч; выпавшие — честное «не_измерено»). ЗАКРЫТ.
- **HIGH-2:** annotate в event_first под fail-soft — сбой информационного поля не роняет боевой
  прогон (протокол несёт «ошибку»). ЗАКРЫТ.
- LOW-1 нормализация актива (strip+upper, запись И чтение) — ЗАКРЫТ; LOW-4 дыры тестов — закрыты
  (интеграция mock event_first, рендер, скан-исключение, кэп, нормализация, строка-кандидат);
  LOW-3 (перечитывание реестра на идею) — бэклог, заболит вместе с ростом реестра (кэп смягчает).

## Кросс-вендор openai/gpt-5.5 — 6 прогонов (reports/cross_review_2026*.md)
1. №1 НЕ ПРОЙДЕН: BLOCKER гонка assign_key (нет лока) + HIGH сиды-выше-реестра (скрытая пересдача) +
   HIGH инъекция registry-dict в обход провенанса + HIGH битая строка валит реестр → все закрыты.
2. №2 НЕ ПРОЙДЕН: HIGH json-null/[]/строка роняли прогон; HIGH объект-без-ключа затенял валидную
   запись → валидация формы записи.
3. №3 ПРОЙДЕН (форма записей).
4. №4 НЕ ПРОЙДЕН: HIGH нормализация только на записи — легаси « ccj.us » не участвовал в «первый
   выигрывает» → нормализация и при чтении.
5. №5 НЕ ПРОЙДЕН: HIGH строка-candidates = iterable символов (фиксация ключа «u») → str = один ключ.
6. **№6 ПРОЙДЕН** — новых BLOCKER/HIGH на боевом пути нет.

## Гейт-чек (буквально)
- (а) Тесты: **523 passed, 2 failed** — оба падения пре-существующий env-долг theme-guard;
  этап: 20 тестов test_attention_field + интеграция.
- (б) Ревью этапа: ПРОЙДЕН (агент — находки закрыты; кросс-вендор №6 ПРОЙДЕН).
- (в) Поток подтверждён: mock event_first — топ-K непуст, `внимание_покрытие` в протоколе,
  поле на всех брифах; ранжирование/суд/seal не тронуты (инвариант проверен агентом по коду
  и автотестом test_field_never_affects_ranking_inputs).
- (г) Слой Б не тронут: пере-ранжирование НЕ включено (П2б, отдельная подпись).
- §11: фоновое табло без изменений; kill-критерий не применим (N < 270).

## Вердикт: ПРОЙДЕН. Тег: gate-P2a.
Замечания в бэклог §R6: LOW-3 (кэш реестра при росте), geo-связка news.yaml (датчик всегда geo='');
покрытие §R5 стартует с ~0 и вырастет после первого канонического фетча (06:45 05.07) — проверить
фактическое покрытие ≥60% на первой неделе наблюдения (метрика в каждом протоколе прогона).
