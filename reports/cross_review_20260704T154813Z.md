# Кросс-вендорное ревью 20260704T154813Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №10 (финальный). Закрыто из №9: (1) bool исключён в РЕАЛЬНОЙ строке вероятности summarize (verdict_line, outcome==УСТОЯЛА); (2) _norm_utc_digits требует T-компоненту — голая дата/мусор → пустой ключ (фикстуры ниже боевых). За 9 прогонов пакета закрыто 2 BLOCKER + 16 HIGH, все с тестами/точечными правками. Финальный вердикт по challenge.py.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:54-57 — `_norm_utc_digits()` всё ещё принимает голую дату как валидный временной ключ, вопреки заявленному контракту «требует T-компоненту; голая дата/мусор → пустой ключ» — контрпример: `_norm_utc_digits("2026-07-04")` через `datetime.fromisoformat()` возвращает `"20260704"`, а не `""`. Такой JSON-фикстурный протокол с `{"ts": "2026-07-04", ...}` будет сортироваться выше реальных прогонов до этой даты, например выше `"2026-07-03T23:59:59Z"`, и `find_idea()` без явного `run_id` может взять не последнюю боевую идею, а фикстуру/полубитый протокол.

- [LOW] orchestrator/challenge.py:85-91 — `_scan_protocols()` принимает любой валидный JSON, но дальше без проверки типа вызывает `pr.get(...)`; один файл `journal/funnel_logs/bad.json` с содержимым `[]` или `"x"` валит весь `list_ideas()/find_idea()` через `AttributeError: 'list' object has no attribute 'get'` — сценарий: в каталоге логов лежит валидный, но не протокольный JSON-артефакт; `_scan_protocols()` его добавляет в `out`, затем `out.sort(key=lambda pr: (_ts_key(pr), str(pr.get("run_id") or "")))` падает.