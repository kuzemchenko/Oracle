# Кросс-вендорное ревью 20260713T124048Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: Этап Э4-каркас программы «Поисковый движок»: перебор мира — LLM-карта сегментов/механизмов БЕЗ тикеров (world_map, суб-потолок 3.00, LLM-числа запрещены), детерминированный скрин сегмент→инструменты (EODHD screener + фолбэк Tier0, ликвидность, sealable), конвейер с возвратом по классифицированной причине (кэпы в config), автоappend кандидат-рёбер в B4 (edge_candidates.jsonl, дедуп эпизода в корме промоушена), пере-скрин вс 09:00. Боевой event_first НЕ переключён. Проверь АДВЕРСАРНО: валидацию LLM-карты (не протекают ли тикеры/числа), квотные алерты, дедуп эпизода promote_edges (не ломает ли легаси-подсчёт N), герметичность (mock не кормит боевой форвард-тест), кэп 40 печатей edge_forward при росте библиотеки.
Файлы: orchestrator/world_map.py, orchestrator/segment_screen.py, orchestrator/world_enum.py, orchestrator/world_enum_dryrun.py, orchestrator/edge_forward.py, ops/promote_edges.py, ops/rescan_maps.py, config/limits.yaml

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] orchestrator/world_enum_dryrun.py:75 — mock/dryrun может кормить боевой B4 forward-test — сценарий: запустить `python orchestrator/world_enum_dryrun.py --append-candidates` без `--candidates-path`; `dry_run()` передаст `write_candidates=True` в `WE.enumerate_event`, а тот допишет пары из фикстуры `ai_power` в production `knowledge/edge_candidates.jsonl`; следующий `edge_forward.run_edge_forward()` по умолчанию читает этот файл и будет тестировать mock-кандидаты как `origin=world_enum`.

- [BLOCKER] orchestrator/world_map.py:72 — валидация LLM-карты не герметична: тикеры и числа проходят внутри строк и top-level полей — контрпример:  
  `{"событие":"AI power VRT.US +12%","сегменты":[{"сегмент":"электрооборудование","порядок":2,"направление":"рост","канал":"capex 2027","механизм":"NUE и GEV выиграют, эффект 15%","секторы":["Industrials"],"индустрии":["Electrical Equipment & Parts"]}],"обоснование":"BNO.US +3%","уверенность":"средняя"}`  
  будет принят: `_segment_problems()` проверяет тикер только в имени сегмента и числовой тип только верхнего уровня сегмента, но не ищет тикеры/числа в `канал`, `механизм`, `индустрии`, `событие`, `обоснование`.

- [HIGH] orchestrator/segment_screen.py:92 — квотные ошибки EODHD в JSON-теле/не-402/429 не дают алерт владельцу и маскируются как пустой скрин — сценарий: `fetch()` возвращает HTTP-200 тело `{"error":"quota exceeded"}` или `{"message":"payment required"}`; `fetch_screener_page()` вернёт `[]`, `screen_segment_api()` вернёт пустой список без `QuotaError`, `notify_owner()` не вызывается, хотя по заданию исчерпание квоты не должно молчать.

- [HIGH] ops/promote_edges.py:83 — дедуп эпизода ломает legacy-подсчёт `N`, потому что выдумывает episode из `sealed_at` — сценарий: две старые независимые однозвенные записи одного ребра без поля `episode`, но с `sealed_at=2026-07-01` и `sealed_at=2026-07-05`; код присвоит `_episode` из даты печати, затем блок `EPISODE_GAP_DAYS=7` отбросит вторую как дубль эпизода. Legacy `N=30` может стать `N=29` и ребро не пройдёт `MIN_OUTCOMES`, хотя явной episode-идентичности в старых данных нет.

- [LOW] ops/rescan_maps.py:48 — требование «пере-скрин вс 09:00» в коде не закреплено — сценарий: оператор/cron запускает `ops/rescan_maps.py` во вторник в 14:00, функция `rescan()` выполнится без предупреждения и запишет протокол; в представленном коде нет ни проверки дня/времени, ни отдельного schedule-файла/guard.