# Кросс-вендорное ревью 20260704T153152Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, ПОДТВЕРЖДАЮЩИЙ прогон №3 (финальный). Закрыто из №2: (1) challenge run_id = ts+pid+внутрипроцессный счётчик; (2) funnel принимает cost_guard, клиент платит в ЦЕПОЧКУ гардов (свой per-run + внешний event_first), пробитие внешнего ре-эскалируется из graceful-стопа; (3) challenge live: RB.precheck(challenge)+RunBudgetGuard, суб-потолки challenge 2.0/6 в limits.yaml. Подтверди закрытие №2. Финальный вердикт; новые находки — только BLOCKER/HIGH с конкретным контрпримером на боевом пути.
Файлы: orchestrator/funnel.py, orchestrator/event_first.py, orchestrator/challenge.py, config/limits.yaml

---

ВЕРДИКТ: НЕ ПРОЙДЕН

Подтверждение закрытия №2:  
(1) `challenge` `run_id = ts+pid+внутрипроцессный счётчик` — закрыто.  
(2) `funnel` принимает `cost_guard`, платит в цепочку гардов, `event_first` ре-эскалирует внешний бюджетный стоп — по представленному пути закрыто.  
(3) `challenge` live получил `RB.precheck("challenge")`, `RunBudgetGuard`, лимиты `challenge: 2.0` и `expected_calls: 6` есть, но закрытие неполное из-за дефекта ниже.

- [HIGH] `orchestrator/challenge.py:201` — бюджетный стоп `challenge` на лету не ловится: обработчик написан как `except Exception`, а `RunBudgetExceeded` в системе заявлен/используется как `BaseException` — контрпример: live `/debate` проходит `RB.precheck("challenge")`, затем во 2–6 LLM-вызове `RunBudgetGuard("challenge", 2.0)` выбрасывает `RB.RunBudgetExceeded`; `except Exception` не срабатывает, API получает необработанное исключение/500 вместо протокола `ОСТАНОВ_бюджет`, разбор не журналируется как корректный бюджетный стоп. Должно быть `except RB.RunBudgetExceeded as _e:` отдельной веткой, как в `funnel.py`/`event_first.py`.