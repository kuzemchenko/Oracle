# Кросс-вендорное ревью 20260704T154233Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №8. Закрыто из №7: инъекция истории в ctx срабатывает при отсутствии ИЛИ None/пустышке ключа (проверка .last, тест с monkeypatch). За 7 прогонов: 2 BLOCKER + 11 HIGH закрыты, каждый с тестом. Финальный вердикт по challenge.py.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:54 — `_norm_utc_digits()` не валидирует timestamp и превращает любой мусор с цифрами в сортировочный ключ — контрпример: в `journal/funnel_logs` лежат реальный протокол `{"ts":"2026-07-04T09:00:02+00:00", ...}` и тестовый артефакт `{"ts":"week9_testday", "этап6_синтез":{"отчёты":[...]}}`; `_ts_key` для второго даст `"9"`, что лексикографически больше `"20260704T090002000000"`, поэтому `find_idea()` возьмёт тестовую идею как «последнюю» вместо боевой.

- [HIGH] orchestrator/challenge.py:256 — инъекция истории проверяет только `quotes[sym]["last"]`, но не проверяет наличие/пустоту `indicators[sym]` — сценарий: `C.build_context()` вернул `ctx={"quotes":{"AAPL":{"last":{"date":"2026-07-03","close":200}}}, "indicators":{"AAPL":None}}`, при этом в `oracle.db` есть история; `_have == True`, блок инъекции не выполняется, и `run_debate()` получает ложное отсутствие индикаторов. Это снова даёт П8-дефект: судья может штрафовать за «нет данных» при наличии данных в БД.

- [HIGH] orchestrator/challenge.py:480 — `summarize()` безусловно пишет пользователю «П10 — судья другого семейства», не проверяя фактические семейства — контрпример: если `DBT.run_debate()` из-за регрессии или замоканного клиента вернёт `{"семейство_генератора":"openai","семейство_судьи":"openai"}`, резюме всё равно заявит соблюдение П10. Это маскирует нарушение ключевого инварианта вместо честного отказа/вынесения вето.

- [LOW] orchestrator/challenge.py:489 — вероятность судьи проверяется через `isinstance(prob, (int, float))`, поэтому `bool` проходит как число — контрпример: `prob=True` в `debate["вердикт"]["вероятность_судьи"]` даст в резюме `вероятность по судье ~100%`, хотя это не числовая вероятность, а булево значение. Для П8 это нечестное число; нужно исключать `bool`, как уже сделано в `_rubric_scores()`.