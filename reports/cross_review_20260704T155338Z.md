# Кросс-вендорное ревью 20260704T155338Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №13. Закрыто из №12 (подтверждено исполнением): _ts_key с невалидным ts падает на run_id-таймстамп; _reports_of терпит этап6_синтез=[] и не-dict отчёты; бюджетный ОТКАЗ журналируется тем же no-clobber хелпером. 557 тестов зелёные. Финальный вердикт.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

Находки:

- [HIGH] orchestrator/challenge.py:65 — `_norm_utc_digits` принимает календарно-невалидный compact timestamp как валидный ключ через regex fallback — контрпример: в `journal/funnel_logs` лежат боевой протокол `run_id="funnel_20260704T090002Z"` и фикстура `run_id="fixture_99999999T999999Z", ts="bad"`. Для фикстуры `_ts_key` вернёт `"99999999T999999"`, она отсортируется новее реального прогона, и `find_idea()/list_ideas()` выберут тестовый артефакт вместо последней боевой идеи.

- [HIGH] orchestrator/challenge.py:299 — бюджетный останов на лету `RunBudgetExceeded` не журналируется при `write=True` — контрпример: live-клиент проходит `precheck`, затем `DBT.run_debate()` бросает `RB.RunBudgetExceeded`; `run_challenge()` возвращает словарь с `"ОСТАНОВ_бюджет"`, но `_write_challenge_file()` не вызывается, файла в `journal/challenges/` нет. Это нарушает заявленный контракт аудируемости «каждый разбор журналируется» и отличается от пред-бюджетного отказа, который пишется.

- [LOW] orchestrator/challenge.py:343 — `_rubric_scores()` падает на не-dict элементах в журнале — контрпример: live-протокол содержит `{"рубрика": {"оценки": ["битый элемент"]}}`; `digest_challenges()` вызывает `_rubric_scores()`, затем `o.get(...)` на строке и получает `AttributeError`, из-за чего еженедельный дайджест не строится.

- [LOW] orchestrator/challenge.py:354 — `_missing_findings()` падает на не-dict находках reviewer’а — контрпример: `{"reviewer_данных": {"judgment": {"находки": ["нет данных по выручке"]}}}`; при `digest_challenges()` строка `f.get("статус")` вызывает `AttributeError`. Для журнального агрегатора это недопустимо: один битый элемент LLM-вывода ломает весь обзор.