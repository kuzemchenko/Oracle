# Кросс-вендорное ревью 20260704T152825Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, ПОДТВЕРЖДАЮЩИЙ прогон №2. Все 5 HIGH из №1 закрыты: (1) _lag_for_pair сканирует directed по всему списку; (2) None-вызовы платны (скользящее среднее известных, прайор 0.10 до первой; cap рвётся — тест); (3) live event_first: build_from_db(asof=asof_date); (4) loader.load_aligned(asof)+on_the_fly(asof) — чувствительности под cutoff, граница v1 снята; (5) challenge run_id+pid и атомарная запись. Подтверди закрытие №1; новые находки — только BLOCKER/HIGH с контрпримером на боевом пути.
Файлы: mathlib/cascade.py, mathlib/calibration/loader.py, mathlib/calibration/sensitivity.py, orchestrator/run_budget.py, orchestrator/cascade_build.py, orchestrator/event_first.py, orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:190 — закрытие №1(5) не подтверждается: `run_id = challenge_{секунда}_{pid}` всё ещё коллидирует для двух challenge-запросов в одном API-процессе в одну секунду; атомарная запись защищает только от битого JSON, но не от перезаписи — сценарий: worker pid=4242 обрабатывает два `/debate` в `20260704T120000Z`, оба получают `challenge_20260704T120000Z_4242`, оба пишут `journal/challenges/challenge_20260704T120000Z_4242.json`, второй протокол полностью затирает первый.

- [HIGH] orchestrator/event_first.py:464 — `RunBudgetGuard` не покрывает самый дорогой боевой участок event-first: `F.run_funnel(...)` вызывается без передачи guard/cost_guard — сценарий: `run_event_first(mode="live", k=3, skip_contour=False)`, precheck создал guard с cap=C, первый `F.run_funnel` тратит `C+ε` внутри своего клиента, но `guard.add()` для этих вызовов не вызывается; event_first не получает `RunBudgetExceeded`, продолжает прогон, а `бюджет_прогона` учитывает только картограф/веттинг, занижая фактический расход.

- [HIGH] orchestrator/challenge.py:207 — live challenge вообще обходит §24 precheck/stop-на-лету: нет вызова `RB.precheck(...)`, нет `RunBudgetGuard`, клиент создаётся напрямую через `OR.make_client(...)`, затем запускается `DBT.run_debate(...)` — сценарий: месячный лимит уже исчерпан по `journal/costs.jsonl`; `run_event_first(mode="live")` отказал бы до первого LLM-вызова, но `run_challenge(..., mode="live")` всё равно начинает состязательный разбор и тратит токены постфактум.