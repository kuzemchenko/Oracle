# Кросс-вендорное ревью 20260704T153816Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, ПОДТВЕРЖДАЮЩИЙ прогон №6. Закрыто из №5: точный матч тикера в find_idea (тест AA≠AAPL, SO≠USO.US); no-clobber запись протоколов challenge (суффикс при коллизии — ничего не перетирается); _ts_key нормализует ISO-смещения к UTC. Это шестой прогон пакета; все находки №1-№5 закрыты. Финальный вердикт по файлу.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] orchestrator/challenge.py:266-272 — no-clobber не является атомарным: проверка `dst.exists()` отделена от записи, поэтому при параллельной коллизии возможна перезапись существующего протокола — сценарий: два процесса в контейнерах с `pid=1`, одинаковой секундой `_now_compact()` и первым `next(_RUN_SEQ)` получают один `run_id`; оба видят `dst.exists() == False`, затем оба вызывают `atomic_write_text(dst, ...)`, последний `os.replace`/атомарный writer перетирает первый файл, несмотря на требование «ничего не перетирается».

- [HIGH] orchestrator/challenge.py:151-155 — точный матч тикера всё ещё не точный для тикеров с точкой: `cand.split(".")[0] == a` срезает любой суффикс, а не только `.US` — контрпример: в протоколе есть идея `{"актив": "BRK.B"}`, вызов `find_idea(asset="BRK")` вернёт `BRK.B`, хотя это другой тикер; аналогично `AA.PR` будет найден по `AA`.

- [HIGH] orchestrator/challenge.py:151-155 — матч «с/без суффикса .US» работает только в одну сторону — контрпример: в протоколе актив `"AAPL"`, пользователь вызывает `find_idea(asset="AAPL.US")`; условия `cand == a`, `cand == f"{a}.US"`, `cand.split(".")[0] == a` все ложны, идея не находится, хотя контракт заявляет совпадение тикера с/без `.US`.

- [LOW] orchestrator/challenge.py:335-336 — фильтр `since` в `digest_challenges` сравнивает ISO-время строками без нормализации смещений к UTC — контрпример: `since="2026-07-04T12:00:00+03:00"` означает `09:00Z`, лог с `ts="2026-07-04T10:00:00Z"` должен попасть в дайджест, но строково `"2026-07-04T10..." < "2026-07-04T12..."`, и разбор ошибочно пропускается.