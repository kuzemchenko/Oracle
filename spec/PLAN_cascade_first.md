# PLAN — каскадо-первичная воронка (event → cascade → instrument)

Статус: ПРЕДЛОЖЕН, ждёт утверждения пользователя. Код не трогается до «да».
Дата: 2026-06-15. Автор: Дирижёр. Канон: MASTER_SPEC.md (§5, §6, §9, §14, §17, §23, §24).

## 0. Зачем
Текущая реализация итерирует **закрытый список из 14 тикеров** (`orchestrator/context.py:29` CORE,
`config/universe.yaml`) и якорит каждый скан к Brent. Это отклонение от канона:
- **§6 Эт.1** требует скан по *событиям/трендам/аномалиям* (200–500), не по тикерам.
- **§17.2** требует *события 1–4 порядка → консенсус → запечатанный прогноз*.
- **§14** задаёт универсум **свойствами** (ликвидно/торгуемо/есть данные/есть шорт), не списком.

Поэтому контур ищет «инструмент → новости про него → каскад» (задом наперёд), упирается в
эффективные Brent-ETF и за 10 живых прогонов выдал 0 идей (см. [[funnel-empty-days-diagnosis]]).

## 1. Принцип: брандмауэр «открытие vs запечатывание»
- **Пространство ОТКРЫТИЯ — открыто.** Скан событий и построение каскадов не ограничены никаким
  списком тикеров. Думать можно о любом эффекте в мире.
- **Пространство ЗАПЕЧАТЫВАНИЯ — ограничено реальностью §9/П16.** Запечатать форвард-прогноз можно
  только на инструмент с источником цены, который мы тянем (EODHD). НО этот набор **динамический**
  (любой тикер EODHD резолвится на лету), а не замороженные 14.
- Узел каскада без торгуемого/разрешимого инструмента → **лист ожидания §17 / research-only**
  (паттерн SPCX), не выбрасывается.

## 2. Амплитуда узла — детерминированная (инвариант 6: математика не LLM)
LLM **только предлагает** причинную связь (узел→узел). Силу считает код:
- **shock_0** — квантованный первоисточник (движение корневого фактора в σ, или интенсивность
  news-кластера в z к фону).
- **sensitivity(node)** — историческая эластичность/бета доходностей инструмента узла к shock корня,
  с лагом из `knowledge/causal_links.yaml` (там уже есть эмпир. corr+lag, напр. USO↔BNO 0.9467 lag0).
  Оценка — walk-forward регрессией на ≥10 лет (§23.1, честная зона), с дисперсией оценки.
- **amplitude(node) = sensitivity × shock_0 × transmission_reliability(цепочки)**.
- **probability(node)** — форвардная вер-ть достичь порога: gaussian_baseline_realized_vol (как в
  `orchestrator/calibrate.py`) ⊗ надёжность звена цепочки.
- **П8:** если sensitivity не оценивается (мало истории / связи нет в библиотеке) → «нет данных»,
  узел без торгуемого утверждения → watchlist/research. Никаких выдуманных бет.

## 3. Этапы сборки (порядок §24; гейт каждого шага обязателен; агентов раньше mathlib не писать)

### Этап 0 — Брандмауэр и каркас (поведение НЕ меняем)
- Развести в коде `discovery_space` (открытое) и `sealable_universe` (динамический реестр §9).
- `CORE` перестаёт быть пределом открытия; остаётся как seed калиброванных бэкграундов.
- **Гейт:** существующие прогоны и тесты зелёные; брандмауэр покрыт тестом; диффом пользователю.

### Этап 1 — Event-first фронт скана (§6 Эт.1)
- Открытый скан событий/трендов/аномалий (GDELT+NewsAPI+pytrends уже подключены; кластеризация на
  широком срезе новостей уже есть, commit 6d56165) → 200–500 сырых сигналов → FDR q<0.1 (§6/§23).
- НЕ привязан к тикеру/Brent.
- **Гейт:** на реальном дне даёт ≥N событий-кандидатов после FDR, полный журнал отсева.

### Этап 2 — mathlib: движок каскадной амплитуды (ядро)
- Расширить `mathlib/tectonic.py`: sensitivity/amplitude/probability по §2, юнит-тесты на синтетике.
- **Гейт:** тесты проходят; путь «нет данных → нет seal» проверен тестом.

### Этап 3 — Калибровка чувствительностей (§23.1)
- Walk-forward калибровка sensitivity+lag на 10 г.; бэкграунды для динамически-резолвнутых тикеров
  считаются на лету. Пины — где статзначимо; «не пинится» — честно, как в текущем `thresholds.yaml`.
- **Гейт:** отчёт калибровки в `ops/reports/`; провенанс каждого параметра.

### Этап 4 — Резолв инструмента (последний шаг, §9)
- Топ-узлы по P×амплитуда → динамический резолв EODHD (история+фон на лету) → seal разрешимых,
  watchlist остальных.
- **Гейт:** узел без инструмента корректно → watchlist; разрешимый → запечатан со всеми полями §9.

### Этап 5 — Промпты агентов (после mathlib!)
- `build_prompts.py` (руками не править): школы рассуждают событие→порядки 1–4 с P+амплитудой на узле,
  инструмент — в конце. П8 «нет данных» сохраняется.
- **Гейт:** промпты регенерируются; инварианты в промптах на месте.

### Этап 6 — Состязательный контур + синтез
- Слепой суд оценивает тезисы по узлам каскада; мэппинг в инструмент ревьюится.
- **Гейт:** полная event-first воронка end-to-end: сперва mock, затем live → либо запечатанная идея,
  либо честный пустой результат с журналом причин (§6).

### Этап 7 — Дашборд/абляция + каденс
- Кроны: заменить brent-якорные прогоны на event-first; дашборд §15 показывает деревья каскадов;
  абляция вклада порядков 1–4.
- **Гейт:** 7 метрик §15 считаются; контрфактический протокол на синтезе (§11.1).

## 4. Что НЕ меняется без отдельной подписи пользователя
- Веса скоринга §7 и состав критериев (22/22/18/14/14/10).
- Рубрика суда и break_threshold 3.0 (смягчать запрещено).
- Денежные ворота §11 и лимиты `config/limits.yaml`.
- П16 форвард-онли запечатывание; слепота суда; развязка семейств П10.

## 5. Первый осязаемый шаг
Этап 0 (брандмауэр + каркас, без смены поведения) → диффом на утверждение. Дальше по гейтам.
