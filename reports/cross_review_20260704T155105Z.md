# Кросс-вендорное ревью 20260704T155105Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, прогон №12. _scan_protocols восстановлен и фильтрует не-dict (полный набор тестов 559 зелёный, включая test_scan_protocols_skips_nondict_json и test_norm_key_rejects_bare_dates_and_garbage — оба проверяют исполнением). Все находки №1-№11 закрыты. Финальный вердикт по challenge.py.
Файлы: orchestrator/challenge.py, orchestrator/tests/test_challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:79 — `_ts_key` не делает fallback на timestamp из `run_id`, если поле `ts` присутствует, но не является валидным timestamp — контрпример: два протокола:  
  `{"run_id": "funnel_20260704T090000Z", "ts": "2026-07-04", "этап6_синтез": {"отчёты": [{"актив": "NEW.US"}]}}` и более старый `{"run_id": "funnel_20260701T090000Z", "ts": "2026-07-01T09:00:00Z", ...}`. Голая дата в `ts` нормализуется в `""`, валидный timestamp из `run_id` игнорируется, поэтому `find_idea()` выберет старую идею вместо последней.

- [HIGH] orchestrator/challenge.py:103 — `_reports_of` доверяет вложенной структуре протокола и падает на dict-протоколе с не-dict `этап6_синтез` — сценарий: в `journal/funnel_logs/bad.json` лежит валидный JSON-dict `{"run_id": "funnel_20260704T090000Z", "этап6_синтез": []}`. `_scan_protocols` его пропустит как dict, а `list_ideas()` / `find_idea()` вызовут `.get("отчёты")` у list и получат `AttributeError` вместо честного пропуска битого протокола.

- [HIGH] orchestrator/challenge.py:107 — элементы `отчёты` не проверяются на dict; один битый элемент ломает поиск/листинг идей — контрпример: `{"этап6_синтез": {"отчёты": ["not-a-report", {"актив": "AAPL.US"}]}}`. В `list_ideas()` первый элемент попадёт в `_fields_of`, где выполняется `report_card.get(...)`, и команда завершится исключением, хотя могла пропустить невалидную карточку и показать валидную.

- [LOW] orchestrator/challenge.py:271 — отказ бюджетной пред-проверки при `write=True` не журналируется, хотя модульный контракт заявляет `Каждый разбор журналируется в journal/challenges/{run_id}.json` — сценарий: live-клиент, `RB.precheck("challenge")` возвращает `{"allowed": False, ...}`; функция возвращает объект с `ОТКАЗ_бюджет`, но файл в `journal/challenges/` не создаётся, аудит попытки отсутствует.

- [LOW] orchestrator/challenge.py:284 — останов на лету `RunBudgetExceeded` тоже не журналируется при `write=True` — сценарий: live-разбор уже начал тратить бюджет, `DBT.run_debate(...)` выбрасывает `RunBudgetExceeded`; функция возвращает `ОСТАНОВ_бюджет`, но не пишет challenge-протокол, поэтому потраченный/оборванный прогон не попадает в журнал и не виден последующему аудиту.