# Кросс-вендорное ревью 20260704T142711Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П2а, ПОДТВЕРЖДАЮЩИЙ прогон №4 (финальный). После твоего №3 ПРОЙДЕН stage-review нашёл и мы закрыли: (HIGH-3, боковой канал) event_scan теперь фильтрует тренды по scan_keywords (только конфиг news.yaml) — ключи реестра «внимания» не порождают кандидат-события и не меняют порог BH-FDR; (HIGH-4) кэп 30 последних ключей реестра в суточном фетче; (HIGH-2) annotate_ideas в event_first под fail-soft try/except; (LOW) нормализация актива strip+upper. Проверь эти четыре фикса на корректность и обходы. Только BLOCKER/HIGH с контрпримером на боевом пути.
Файлы: orchestrator/attention_field.py, data/trends.py, orchestrator/event_first.py, orchestrator/event_scan.py, orchestrator/tests/test_attention_field.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/attention_field.py:78 — нормализация актива сделана только на входе/записи, но не при чтении реестра; старые/неканонические записи реестра не участвуют в «первый выигрывает», что даёт пересдачу ключа на боевом пути — контрпример: в `journal/attention_keys.jsonl` уже есть строка, созданная до фикса или ручным append-only импортом: `{"актив":" ccj.us ","ключ":"uranium squeeze","источник":"old","run_id":"r1","ts":"..."}`. Затем live `event_first` получает картограф-идею по `CCJ.US` с `candidates=["другой ключ"]`. `field_for_asset()` нормализует вход в `CCJ.US`, `_load_registry()` кладёт запись под ключом `" ccj.us "` без `strip().upper()`, `resolve_key()` не находит старую запись, `assign_key()` дописывает новую `{"актив":"CCJ.US","ключ":"другой ключ",...}`. Итог: для одного актива два ключа, запрет пересдачи и провенанс «первый окончательный» нарушены.