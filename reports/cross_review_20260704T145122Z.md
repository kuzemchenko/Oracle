# Кросс-вендорное ревью 20260704T145122Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П3, ПОДТВЕРЖДАЮЩИЙ прогон №2. Закрыто из №1: (BLOCKER) устаревший прогон (>3дн) → честный ОТКАЗ без идей; (HIGH) битый последний протокол → ОТКАЗ без подмены старым; (HIGH) дедуп = явное правило «один актив — одна карточка (старший трек)» + счётчик + пометка также_в; (HIGH) _dict-коэрция против кривых форм; (HIGH) УСТОЯЛА_БЕЗ_КРИТИКА вне числителя и знаменателя (отдельный счётчик); (LOW) retention по 7 дн всегда; отказы не журналируются. Подтверди закрытие; новые находки — только BLOCKER/HIGH с контрпримером на боевом пути.
Файлы: orchestrator/partner_session.py, orchestrator/tests/test_partner_session.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] orchestrator/partner_session.py:131 — freshness-gate пропускает протокол с нечитаемым или будущим `ts`: `age is None` не ведёт к отказу, отрицательный возраст тоже проходит — контрпример: `PS.build_session({"run_id":"ef_old","ts":"not-a-date","граф_отбор":{"money_трек":[{"актив":"GEV.US"}]}}, asof="2026-07-04T18:00:00+00:00")` возвращает идею с `"возраст_прогона_дней": None`, а не честный отказ; аналогично `ts="2099-01-01T00:00:00+00:00"` даёт отрицательный возраст и идеи. Закрытие stale/BLOCKER не подтверждается: свежесть не доказана, но идеи отдаются.

- [HIGH] orchestrator/partner_session.py:158 — `_dict`-коэрция не закрывает кривые формы списковых полей; валидный JSON с не-list значением валит боевой путь сборки сессии — контрпример: `PS.build_session({"ts":"2026-07-04T09:00:00+00:00","граф_отбор":{"money_трек":1}}, asof="2026-07-04T18:00:00+00:00")` падает `TypeError: 'int' object is not iterable`; аналогично `провизорный_трек: 1` и `картограф_идеи: 1`. Закрытие HIGH по кривым формам не подтверждается.