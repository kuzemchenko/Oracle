# Кросс-вендорное ревью 20260704T153332Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: НОЧНОЙ ПАКЕТ, ПОДТВЕРЖДАЮЩИЙ прогон №4. Закрыто из №3: challenge ловит RB.RunBudgetExceeded отдельной веткой (BaseException). Финальный вердикт.
Файлы: orchestrator/challenge.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] orchestrator/challenge.py:203 — `_RUN_SEQ += 1` не потокобезопасен, поэтому заявленный разводчик `run_id` внутри одного процесса не гарантирует уникальность — сценарий: два параллельных `/debate` в одном threaded/API-процессе в одну и ту же секунду оба читают `_RUN_SEQ == 41`, оба записывают `42`, получают одинаковый `challenge_YYYYMMDDTHHMMSSZ_<pid>_42`; далее `atomic_write_text` пишет в один и тот же файл, один протокол теряется/перетирается.

- [HIGH] orchestrator/challenge.py:241 — ветка `except RB.RunBudgetExceeded` возвращает протокол останова, но не журналирует его при `write=True`, в отличие от успешного разбора — сценарий: live-разбор сделал несколько LLM-вызовов, превысил cap, выброшен `RunBudgetExceeded`; пользователь получает `"ОСТАНОВ_бюджет"`, но в `journal/challenges/{run_id}.json` записи нет, нарушается заявленный инвариант файла «Каждый разбор журналируется… ничего не удаляется» и аудит бюджетных остановов исчезает из истории.

- [LOW] orchestrator/challenge.py:59 — `_ts_key()` не нормализует ISO-время к UTC, а просто приклеивает цифры offset’а к ключу, поэтому «хронологический» порядок может быть неверным — контрпример: `2026-07-04T10:00:00+03:00` фактически равен `07:00Z`, но ключ `20260704T1000000300` сортируется позже, чем `2026-07-04T08:00:00+00:00` с ключом `20260704T0800000000`; `find_idea()` без параметров может взять не последнюю по реальному времени идею.