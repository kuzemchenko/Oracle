# Кросс-вендорное ревью 20260704T142847Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П2а, ПОДТВЕРЖДАЮЩИЙ прогон №5. Закрыто из №4: _load_registry нормализует актив (strip+upper) и при ЧТЕНИИ — легаси-запись « ccj.us » выигрывает для CCJ.US, пересдача через регистр невозможна (тест добавлен). Подтверди закрытие №4; новые находки — только BLOCKER/HIGH с контрпримером на боевом пути.
Файлы: orchestrator/attention_field.py, orchestrator/tests/test_attention_field.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

№4 подтверждаю: закрыто — `_load_registry` нормализует актив при чтении через `strip().upper()`, легаси-запись `" ccj.us "` выигрывает для `CCJ.US`, пересдача через регистр этим путём не проходит.

[HIGH] orchestrator/attention_field.py:141 — строковый `candidates` интерпретируется как iterable символов, навсегда журналируя первый символ вместо ключа — боевой путь: `annotate_ideas(..., картограф_идеи=[{"актив":"CCJ.US", "ключи":"uranium squeeze"}], ...)` передаёт строку в `field_for_asset`; `list("uranium squeeze")` даёт `["u", ...]`, `assign_key` фиксирует для `CCJ.US` ключ `"u"`. Даже если в БД есть Trends по `"uranium squeeze"`, поле станет `не_измерено` по `"u"`, а следующий прогон с `["uranium squeeze"]` уже не сможет исправить ключ из-за правила «первый выигрывает».

[HIGH] orchestrator/attention_field.py:88 — пустой актив допускается к журналированию, но затем сам же реестр такую запись не читает — боевой путь: `annotate_ideas(..., картограф_идеи=[{"ключи":["uranium squeeze"]}], ...)` при отсутствии `"актив"` превращает asset в `""`; `assign_key` пишет `{"актив":"", "ключ":"uranium squeeze", ...}`. `_load_registry` потом отвергает эту строку как невалидную, поэтому следующий прогон с тем же отсутствующим активом и кандидатом `["copper"]` запишет ещё одну строку. Итог: append-only журнал загрязняется незагружаемыми назначениями, а запрет пересдачи фактически не действует для этого боевого входа.