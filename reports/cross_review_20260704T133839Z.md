# Кросс-вендорное ревью 20260704T133839Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П1-гейт (REVISION_2026-07 §R4.1): датчик перегретости Trends перед подключением в контур. Закрыты три края: (а) парная фильтрация значений и partial-флагов; (б) расчёт только по последнему фетчу fetched_at (лоскутная ре-нормировка окон Trends давала ложные фазы); (в) новая фаза ОТЫГРАНО для сдувшегося ДАВНО хайпа (раньше читался как РАНО — инверсия). Канонический TRENDS_TIMEFRAME=today 3-m. Ищи дефекты математики фазовой логики, краевые ряды с неверной фазой, дыры тестов, проблемы SQL rows_for_attention.
Файлы: mathlib/attention.py, mathlib/tests/test_attention.py, data/trends.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [BLOCKER] data/trends.py:93 / data/trends.py:163 — `timeframe` не сохраняется в БД и не фильтруется в `rows_for_attention`, поэтому неканонический фетч отравляет контур, а `attention_score` потом ложно маркирует результат как `today 3-m` — сценарий: сначала штатно загружен `brent oil` с `today 3-m`, затем оператор запускает `data/trends.py --keyword "brent oil" --timeframe "today 12-m"`. `store()` пишет строки в ту же таблицу `trends` без поля timeframe, `rows_for_attention()` выбирает `MAX(fetched_at)` и отдаёт 12-месячную нормировку, а `attention_score()` возвращает `"окно_trends": "today 3-m"`. Это нарушает канон `TRENDS_TIMEFRAME=today 3-m` и П8: число выглядит валидным для 3 месяцев, но рассчитано по другой шкале.

- [HIGH] mathlib/attention.py:190 — `peak_idx = int(np.argmax(a))` берёт первое вхождение максимума, из-за чего свежий blow-off после плато пика классифицируется как `ОТЫГРАНО`, а не `ЛОВУШКА` — контрпример: `interest = [20, 30, 60, 100, 100, 100, 100, 35, 20, 20]`. Последний пик 100 был на индексе 6, расстояние до конца `9−6=3`, при `r=4` это недавний пик, значит после падения до 20 должна быть `ЛОВУШКА`. Код берёт первый пик на индексе 3, расстояние `9−3=6 > 4`, считает пик “давним”, `level=median([20,20])/100=0.2 <= 0.40`, `smooth_last=20 <= 0.50*100=50`, `near_peak_pts=4`, и выдаёт `ОТЫГРАНО`.

- [HIGH] mathlib/attention.py:143-145 / mathlib/attention.py:224 — нарушен заявленный контракт выхода: в успешном результате нет ключа `"компоненты"`, а `score` считается только как `level`, хотя в модульном контракте написано “среднее доступных компонент (уровень + аномалия)” — сценарий: downstream-код, одинаково обрабатывающий `score=None` и успешный ответ, делает `r["компоненты"]`; для ошибок ключ есть (`{}`), для нормального ряда, например `[10,20,30,40,50,60,70,80]`, будет `KeyError`. Дополнительно два ряда с одинаковым последним уровнем, но разной аномальностью, получают одинаковый `score`, потому что аномалия вообще не входит в расчёт.

- [HIGH] data/trends.py:104 / data/trends.py:163 — последний успешный пустой фетч не фиксируется, поэтому `rows_for_attention()` может вернуть устаревший ряд вместо честного “нет данных” — сценарий: вчера для keyword были строки с `fetched_at=T1`; сегодня `fetch_keyword()` получил пустой `iot`, `rows_iot=[]`, `store()` ничего не пишет и не создаёт маркер фетча `T2`; затем `rows_for_attention()` выбирает `MAX(fetched_at)=T1` и датчик рассчитывает фазу по старой нормировке. По П8 при последнем фетче без ряда должен быть `score=None`/“нет данных”, а не старая фаза.

- [LOW] mathlib/tests/test_attention.py:50-55 — тесты не покрывают плато максимума с последующим свежим падением, поэтому дефект с `np.argmax()` не ловится — существующий `test_blowoff_rollover_is_trap` использует одиночный пик `[... 100, 95, 78, 60, 45]`; нужен тест с повторяющимся максимумом, например `[20,30,60,100,100,100,100,35,20,20]`, где ожидаемая фаза `ЛОВУШКА`, а текущий код выдаёт `ОТЫГРАНО`.