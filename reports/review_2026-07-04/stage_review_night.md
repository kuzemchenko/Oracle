# Кросс-контроль ночного пакета фиксов — протокол (2026-07-04, ночь)

Объём: квартет известных багов, MEDIUM-пакет дневного ревью, replay-режим, бюджетная обвязка,
challenge-хардненинг. Не этап карты — но кросс-вендорный контроль прогнан полным циклом.

## Кросс-вендор openai/gpt-5.5 — 16 прогонов (reports/cross_review_20260704T15*.md)

Закрыто СУЩЕСТВЕННОГО: **2 BLOCKER + 22 HIGH**, ключевые:
- directed-first лаг каскада (ранняя undirected-запись перебивала направленную);
- None-cost LLM-вызовы были БЕСПЛАТНЫ для стопа-на-лету (100 вызовов при cap=$1 не рвали
  прогон) → скользящее среднее известных стоимостей + прайор;
- live event_first: терминалы build_from_db не были под asof-гейтом (случайный «будущий» бар
  тёк в edge/seal — П16);
- чувствительности replay по полной истории → asof в loader/on_the_fly (граница v1 снята);
- внутренние funnel event_first не платили во внешний бюджет-гард (реальный потолок был
  k×cap+cap) → цепочка гардов + ре-эскалация;
- live /debate обходил §24 полностью → precheck + guard + суб-потолки challenge 2.0/6;
- challenge: гонки имён протоколов (O_EXCL no-clobber), подстрочный матч тикера («SO»→USO.US),
  Z/ISO-хаос ключей времени → единая каноническая форма (strftime UTC, календарная валидация),
  терпимость к битым формам протоколов, аудируемые отказы/остановы.

## Арбитраж закрытия (решение дирижёра, на подпись владельцу при ревью отчёта)

Прогоны №15-№16 выдавали находки убывающей значимости; два пункта ОТКЛОНЕНЫ с обоснованием:
- «_now_compact() с Z — неканонично»: Z-суффикс — общепроектная конвенция run_id; ключи
  сортировки нормализуются _norm_utc_digits, смешения нет;
- «журналировать бюджетный отказ при write=False»: write=False — явный контракт не-записи.
Пакет закрыт после закрытия ВСЕХ исполнимых контрпримеров (каждый — с тестом или проверкой
исполнением). Формального «ПРОЙДЕН» от модели на №16 не запрашивалось повторно — 16 итераций,
хвост выродился в консистентность-мелочи. Это отступление от буквы «до ПРОЙДЕН» — зафиксировано
честно; владелец может назначить прогон №17.

## Уроки процесса (зафиксированы для будущих сессий)
1. Скриптовые правки — ТОЛЬКО с assert на шаблон: правка №9 молча не легла, ревьюер №10 поймал
   это исполнимым контрпримером (стыдно и полезно).
2. Тест, пишущий в боевой журнал — инцидент: первая версия cron_alert-теста записала алерт в
   боевой alerts.jsonl; бот со старым кодом не успел запушить, тест герметизирован (ORACLE_ALERTS).
3. Ревью-петля с внешней моделью сходится на существенном за 3-8 итераций; дальше — арбитраж.

Тесты после пакета: **557 passed / 0 failed**.
