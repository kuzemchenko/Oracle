# Кросс-вендорное ревью 20260704T135846Z

Модель: openai/gpt-5.5 (роль critic, чужое семейство)
Задание: П1-гейт, ПОДТВЕРЖДАЮЩИЙ прогон №4 (финальный). Закрыто из №3: related Breakout → _related_rows/_related_value best-effort с None; последний фетч выбирается хронологически (naive=UTC, тай-брейк строкой, нечитаемые метки — строковый фолбек). Ранее закрыто: NULL-легаси timeframe, гарантия свежести без обхода, naive/aware, миграция в коде, argmax по последнему касанию, смежность точек хайпа, единый контракт. Подтверди закрытие №3 и скажи, есть ли ЕЩЁ блокеры/HIGH. LOW-бэклог goals.md не повторяй.
Файлы: mathlib/attention.py, mathlib/tests/test_attention.py, data/trends.py, data/news_common.py, ops/tests/test_trends_rows.py

---

ВЕРДИКТ: НЕ ПРОЙДЕН

- [HIGH] data/trends.py:151 — №3 закрыт не end-to-end: `rows_for_attention()` всё ещё выбирает последний фетч через SQL `MAX(fetched_at)` лексикографически, а не хронологически; исправление есть в `attention_from_rows()`, но сюда оно не применяется — контрпример: для одного `keyword/timeframe` есть фетчи `"2026-07-04T10:00:00+02:00"` (=08:00 UTC) и `"2026-07-04T09:00:00+00:00"` (=09:00 UTC). SQL `MAX` выберет строку `10:00+02:00`, то есть более старый фетч; `attention_from_rows()` уже не сможет восстановить правильный выбор, потому что свежие строки отфильтрованы до него.

- [HIGH] data/trends.py:69 — `_call()` превращает любой устойчивый сбой pytrends в `RateLimited`, а `run_all()` дальше печатает “Google 429”; это нечестная диагностика и маскировка поломки источника/клиента под внешний лимит — сценарий: `pt.interest_over_time()` 4 раза падает из-за изменения API/`KeyError`/таймаута не-429; код поднимет `RateLimited(str(last))`, `run_all()` посчитает это `rate_limited += 1` и выведет “Google 429”, хотя 429 не было.