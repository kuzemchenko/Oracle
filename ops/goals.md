# Готовые /goal по плану §24 — копируй в Claude Code построчно, по одной цели на сессию.
# После достижения: /gate-check → если ПРОЙДЕН: git tag, /goal clear, /clear, следующая строка.

[x] Нед.1: /goal Каркас репозитория по §28 создан, config/models.yaml limits.yaml weights.yaml thresholds.yaml заполнены из §26 и §30, коннектор EODHD качает дневные котировки Brent, меди, SPY, DBC с историей 10 лет в storage/oracle.db
[x] Нед.2: /goal Коннекторы GDELT и NewsAPI.ai работают, pytrends подключен, суточный новостной поток нормализуется и тегируется (язык, страна, тип, время) без ручной правки, дедупликация работает
[x] Нед.3: /goal mathlib собран целиком (Brier по корзинам, FDR Бенджамини-Хохберга, запечатывание hash, сверка исходов, индикаторы, Келли с shrinkage, проверка лимитов) и все pytest в mathlib/tests зелёные, запись в predictions.jsonl только append через mathlib.seal()
    # gate ПРОЙДЕН (§24: 59 тестов зелёные; прогноз неизменяем — Edit/bash>> блокируются guard_journal, verify ловит подмену).
    # CARRY-FORWARD к Нед.5-6: «разметка волн (Эллиотт)» из списка §24 в mathlib НЕ реализована — нужна до подключения агента-волновика.
[x] Нед.4: /goal Программа §23.1 выполнена: фоновые дисперсии и FDR-порог в thresholds.yaml, издержки по инструментам ядра, пороги тайминга и детекторы манипуляций откалиброваны walk-forward c отчётами, knowledge/causal_links.yaml содержит 30+ связей с лагами и интервалами, knowledge/precursors.yaml построен по 30+ большим движениям
    # gate ПРОЙДЕН (§24, аудит v2 принят пользователем): walk-forward по всем детекторам в ops/reports/week4/ (REPORT.md + *.json);
    #   mathlib/calibration/* + 19 тестов зелёные (130 всего); thresholds.yaml calibrated=true.
    #   РЕВИЗИЯ ПОСЛЕ РУЧНОГО АУДИТА (все доходности/движения на adjusted_close; costs на raw):
    #   - убраны сплит-артефакты USO 1:8 (2020-04-29) и COPX (2015-11-18); каталог пересобран, макс. ход теперь 58%.
    #   - spent_move_sigma ЭМПИРИЧЕСКИ НЕ ПИНИТСЯ: на adjusted продолжение слабо положительно на всех ≤2.5σ,
    #     порога «смерти» нет (raw-порог 0.75σ был артефактом сплитов) → удержан ориентир 1.5σ; volume_spike_z=null.
    #   - false_breakout revert_bars=1 (устойчив); stop_hunt max_pen_atr=3.0 — слабая идентификация.
    #   - causal_links 33 (9 эмп.+24 домен): зеркальные пары схлопнуты (undirected); ВСЕ эмп. лаги=0.
    #   - precursors 48 движений; base_rate=0.30 у всех — следствие квантильных порогов (hi_q .70/lo_q .30),
    #     значим только freq_before_move vs 0.30 (=lift); volume_buildup 2.1, deep_drawdown 1.96.
    #   Честно «нет данных» (П8): OI, IV, глубина стакана, ставка шорта, похожесть нарративов, фон частот слов.
    # CARRY-FORWARD: (1) разметка волн Эллиотта в mathlib (из Нед.3) — до агента-волновика Нед.5-6.
    #   (2) ЭМПИРИЧЕСКИЕ ЛАГИ КАСКАДОВ НЕ ИЗМЕРИМЫ на 8 ETF (все лаги=0, ряды синхронны) — нужны товарные
    #     ряды (пшеница, газ, удобрения и т.п.); до их добавления доменные causal-лаги калибруются ТОЛЬКО форвардом.
    #   (3) форвард-валидация доменных causal-связей, stop_hunt и spent_move_sigma; (4) фон частот слов и
    #     событийные предвестники — после накопления истории новостей.
    # РЕШЕНО в ходе аудита (раунд 2):
    #   - битый тик: НЕ winsorize — построен точечный детектор mathlib/calibration/dataquality.detect_bad_ticks
    #     (выброс+полный откат+спокойные пиры); поймал CPER 2015-02-02, исключил 2 движения из каталога с записью причины.
    #   - brent_canonical_source: EODHD прозвонен — фьючерса Brent НЕТ (COMM=404). РЕШЕНО пользователем = BNO.US
    #     (ETF-прокси; риск-агент и синтезатор обязаны помечать roll/contango+трекинг-ошибку). PENDING_USER ЗАКРЫТ.
[x] Нед.5-6: /goal Промпты ВСЕХ агентов блоков B,C,D,G лежат в agents/prompts (в каждом П8), оркестратор воронки вызывает OpenRouter по models.yaml с фолбеками, сквозной тестовый прогон собирает поле суждений всех школ в стандартном формате Дирижёра
    # gate ПРОЙДЕН (§24: «все школы выдвигают кандидатов, поле суждений в стандартном формате»).
    #   ЧТО СОБРАНО:
    #   - 21 промпт в agents/prompts/{id}.md — блоки B(10)+C(4)+D(2)+G(5); в КАЖДОМ дословно П8 и
    #     контракт вывода §5.2. Генерятся agents/build_prompts.py из agents/registry.py (build_prompts --check
    #     ловит ручной дрейф). Инвариант 1 CLAUDE.md проверяется и сборщиком, и тестом, и agents.load_prompt.
    #   - orchestrator/: openrouter.py (роли+фолбеки §26, П10 judge_family_ok, лог стоимости→journal/costs.jsonl,
    #     лог смены модели→funnel_logs; LiveClient + MockClient), judgment.py (поле §5.2 + ПРОГРАММНАЯ П8-ворота),
    #     context.py (срез из oracle.db+knowledge, §5.5 непересекающиеся проекции, теханализ через mathlib.indicators),
    #     agents.py, funnel.py (поле суждений + карта противоречий §5.4 + контрфактический протокол §11.1 +
    #     процедурное вето §5.6), run.py (cron-вход §27).
    #   - models.yaml: добавлен school_roles (заполнение пробела §26 — §26 даёт архетипы, не все 10 школ);
    #     семьи РАЗНЕСЕНЫ для независимости §5.5 (anthropic/openai/google/x-ai/deepseek/qwen). ИНЖЕНЕРНОЕ
    #     решение, помечено в конфиге — состав/семьи школ меняются только с согласия пользователя.
    #   ПРОГОНЫ:
    #   - mock (без сети/трат): 21/21 агентов, 12/12 школ, поле §5.2 полное, кандидаты, противоречия, абляция.
    #   - LIVE OpenRouter (funnel_20260611T135205Z): 21/21 ок, 12/12 школ реальными моделями 6 семейств,
    #     7 кандидатов; школы подняли реальную новость (удары США по Ирану/танкеры) и построили тезисы,
    #     fundamental/elliott/cyclist/историки честно «нет данных» (П8 как задумано); все П8-чистые;
    #     стоимость $0.78 < бюджет funnel_full $8.00 (limits.yaml). Тесты: 150 зелёных (17 в orchestrator/tests).
    #   ВАЖНО (интерпретация гейта): «все школы выдвигают кандидатов» = механизм работает и каждая школа
    #     УЧАСТВУЕТ в поле; принуждать кандидата от каждой школы каждый прогон ЗАПРЕЩЕНО (нарушило бы П8 и
    #     §6 «<30 кандидатов = слабый день — легитимно»). В mock все школы с данными предлагают; в live —
    #     6/12 предложили на реальной новости, остальные честно воздержались.
    #   CARRY-FORWARD к Нед.7: (1) состязательный контур E (слепой судья, рандомизация, рубрика) + ЖЁСТКАЯ
    #     проверка П10 judge≠family(generator) на КАЖДОЙ идее; (2) экстремизация §5.5 с поправкой на корреляцию
    #     (сейчас агрегат = простое среднее чистых голосов, помечено TODO в funnel.counterfactual_protocol);
    #     (3) риск-агент, портфель, скоринг §7, синтезатор §8; (4) проверка per_run_token_budget ПЕРЕД прогоном
    #     (mathlib.check_run_token_budget) — сейчас стоимость только логируется постфактум.
    #   CARRY-FORWARD из Нед.3-4 ЗАКРЫТ: разметка волн Эллиотта РЕАЛИЗОВАНА в mathlib/waves.py
    #     (zigzag_pivots, fib_retracement, nearest_fib, label_impulse с 3 жёсткими правилами R1/R2/R3,
    #     label_correction, wave_markup) + 16 тестов зелёных (mathlib/tests/test_waves.py; всего 166).
    #     Подключено в context.py: волновик получает waves.{symbol} (пивоты, проверка правил, фибо).
    #     LIVE-проверка (grok-4.3): волновик ИНТЕРПРЕТИРУЕТ разметку — цитирует waves.BNO.US/USO.US.impulse_last,
    #     видит нарушения R3/R1 и честно отдаёт «нет данных» (валидного счёта нет) с основаниями (П8-чисто) —
    #     это его работа, а не следствие отсутствия тулкита. Порядок сборки восстановлен (mathlib перед агентом).
    #     Остальные carry-forward (товарные ряды для лагов, форвард-валидация) — без изменений.
[x] Нед.7: /goal Состязательный контур работает: слепота, рандомизация порядка, версионируемая рубрика, вопрос "кто продаёт нам"; риск-агент, портфельный менеджер, скоринг и синтезатор дают полный цикл этапов 1-6 воронки на тестовом дне
    # gate ПРОЙДЕН (commit 30f6129 «week 7 done»; отмечено РЕТРОСПЕКТИВНО 2026-06-12 — бухгалтерия
    #   goals.md отставала). Верифицировано: полный цикл §6 этапов 1–6 зелён в test_full_cycle_* —
    #   слепой судья + рандомизация порядка, П10 judge≠family(generator) на каждой идее, рубрика
    #   versioned-файл с пересчётом вердикта кодом, процедурное вето §5.6, риск-агент (short_borrow),
    #   скоринг §7, портфель, синтезатор §8 (13 полей), пред-проверка бюджета per_run. Детали — память
    #   [[week7-adversarial-synthesis]].
[x] Нед.8: /goal Маскированные кейсы в knowledge/masked_cases дают >=70% по рубрике без нарушений П8, дашборд показывает все метрики §15, абляционная отчётность по контрфактам работает
    # gate ПРОЙДЕН (§24, /gate-check 2026-06-12; commit 5064831, tag week-8):
    #   - masked LIVE (masked_20260612T122222Z): 92% (12/13), 13 кейсов, ВСЕ чисты по П8, ВСЕ 5 reject
    #     решены верно (жёсткая защитная дисциплина). Порог 3.0 НЕ смягчался — путь с 77%→92% через
    #     обогащение кейсов + починку контура (решения пользователя), не через планку.
    #   - дашборд §15: все 7 метрик в dashboard/metrics.json+index.html (калибровка/hit_rate_pnl/
    #     стоимость_инсайта/статус_воронки/holdout/реестр_версий/предотвращённые_ошибки=60).
    #   - абляция §11.1: journal/proposed_adjustments.md (drop-one по 4 прогонам; вклад в Brier
    #     «накапливается, N<30» — честно, форвард-исходов 0). Тесты: 258 зелёных.
    #   ЧЕМ ПОДНЯЛИ (честно, П16): mc11-ловушка — реальные встречные ветры шорта (бэквордация=−carry,
    #     заёмка не задана, импульс держится мес.) → net 4→1, РАЗБИТА. mc09 — бета/проходимость ПРОКСИ
    #     (маскировка вычистила торгуемость) → net 1→3. mc10 — форвард-ре-рейтинг 3–6кв + фундам.
    #     инвалидация (снят конфликт горизонт<лаг и тугой ATR-стоп). Един. незачёт mc05 (56.7%) — кейс
    #     не трогали, стохастика судьи на границе; gate не рушит (на это и расширяли набор 6→13).
    #   РИСК-МОДУЛЬ боевой воронки (отдельный реальный баг, mc10 вскрыл): synthesis._idea_horizon_days/
    #     _parse_horizon_text — net-асимметрия и издержки шорта масштабируются по ГОРИЗОНТУ тезиса
    #     (~√время), а не фикс 10д/ATR; backward-compat 'недели'→10д (якорный тест), +4 теста.
    #   CARRY-FORWARD к Нед.9: (1) набрать форвард-исходы — абляция/калибровка/§15 hit-rate/P&L пока
    #     «накапливается» (N<30, разрешённых прогнозов 0); (2) mc05/mc10 — пограничные affirm (~3.0),
    #     при обновлении pinned-моделей перепроверить регрессией; (3) бумажный режим §11 (270 прогнозов
    #     ИЛИ подпись пользователя) — ворота С→Б ещё впереди.
    # NB: Нед.7 фактически выполнена (commit 30f6129; полный цикл §6 этапов 1–6 зелён в
    #   test_full_cycle_*), но строка [ ] Нед.7 выше НЕ отмечена — бухгалтерия goals.md отстала.
[x] Нед.9: /goal Первый боевой прогон выдал отчёт со всеми 13 полями §8, первые прогнозы запечатаны, калибровочный режим запущен, тематический режим Brent настроен, /gate-check подтверждает Gate С→Б
    # gate ПРОЙДЕН (§11 С→Б, /gate-check 2026-06-12; решение пользователя — вариант A). Все 4 критерия §11(стр.191):
    #   (1) программа §23 — Нед.4; (2) сквозной live-прогон полной воронки БЕЗ нарушений П8 —
    #       funnel_20260612T140324Z (live, тема brent): 21/21 агентов, 12/12 школ, 7 кандидатов,
    #       противоречий 0, вето/П8 0 (проверено покомандно: ВСЕ 21 поля violations пусты, вето []),
    #       стоимость $0.80<$8 cap; (3) masked ≥70% — Нед.8 92%; (4) запечатывание работает.
    #   ЧЕСТНО (П16, не смягчаю): live-прогон 12.06 — ЛЕГИТИМНЫЙ слабый день §6 (FDR/фильтры срезали
    #     все 7 → 0 идей), поэтому live исполнились ТОЛЬКО этапы 1–4; этапы 5–6 (дебаты/синтез/
    #     запечатывание funnel_forward + отчёт 13 полей) доказаны на masked (Нед.8, 92%) и test_full_cycle
    #     (Нед.7), НЕ на живой воронке. Пользователь принял трактовку A: «13 полей live» — артефакт
    #     удачного дня, а не критерий §11; машина 5–6 доказана, гейт закрыт.
    #   ЗАПЕЧАТАНО live: 18 калибровочных §9-прогнозов (calibrate, 6 активов ядра × 3 порога {close,±0.5σ_H},
    #     resolve_by 2026-06-19), hash-цепь цела; вероятность = ЧЕСТНАЯ базовая линия (гаусс/realized vol),
    #     НЕ LLM-edge (помечено prob_model). Исходов 0 (форвард, срок не вышел) — до ворот Б→Д 270.
    #   СОБРАНО Нед.9: orchestrator/forecast.py (§9-прогноз из идеи + seal ДО показа, инвариант 3),
    #     resolve.py (сверка исходов §10.10 → ОТДЕЛЬНЫЙ journal/outcomes.jsonl, predictions.jsonl неизменяем),
    #     calibrate.py (§17.3 недельные прогнозы), funnel.stage6 запечатывает funnel_forward live + рендерит
    #     13 полей §8, run.py режимы funnel/theme/calibrate/resolve, крон §27 (calibrate Вт/Пт, theme brent
    #     будни, resolve ежедн., funnel Пн). Тесты: 267 зелёных (+9 Нед.9).
    #   CARRY-FORWARD к Нед.10+: (1) ДОЖДАТЬСЯ живого дня §6 с идеей до этапа 6 — первый live funnel_forward
    #     §9-прогноз + отчёт 13 полей вживую (крон funnel Пн это и даёт); (2) набор форвард-исходов к воротам
    #     Б→Д (270 разрешённых + калибровка ±10пп + превышение бенчмарка) — НЕ С→Б, это следующие ворота;
    #     (3) NewsAPI.ai/EODHD ключей в env НЕТ — live идёт на GDELT+oracle.db; добавить ключи для полноты новостей.

# ════════════════════════════════════════════════════════════════════════════════════
# ДОРОЖНАЯ КАРТА ФИКСОВ (после мультиагентского ревью 28.06.2026) — детали spec/FIXPLAN_2026-06-28.md
# Причина: за всё время 0 инвест-рекомендаций. Диагноз: скан/генерация здоровы, ломается финал
# воронки (суд→seal→выдача) + рассинхрон контрактов модулей. Кросс-ссылки: A#=flow-ревью,
# §N.M=код-ревью (отчёты wf_777106af / wf_8970337a).
#
# ⚙️ ЦИКЛ КАЖДОГО ЭТАПА (за счёт /goal, обязателен — см. /gate-check):
#   1. открыть /goal этапа Fn;  2. кодить + настраивать flow;  3. ОБЯЗАТЕЛЬНОЕ РЕВЬЮ ЭТАПА —
#   Workflow {name:"stage-review", args:{stage:"Fn", base:"<тег/коммит начала этапа>"}} (flow+код
#   по диффу → скептик → вердикт ПРОЙДЕН/НЕ ПРОЙДЕН + проверка «идеи потекли»);  4. починить
#   blockers/регрессии до ПРОЙДЕН;  5. /gate-check (тесты зелёные + stage-review ПРОЙДЕН + перепрогон
#   event_first: суд не валит 100%, идеи доходят);  6. git tag fix-Fn, закрыть /goal → следующий.
#   БЕЗ всех 5 пунктов этап не закрывать (нулевая автономия по порогам — слой Б только с подписью).
# ════════════════════════════════════════════════════════════════════════════════════

[x] F0 (P0 блокеры money-пути + инварианты): /goal суд перестаёт валить 100% и идеи доходят до пользователя; gate stage-review F0 ПРОЙДЕН + перепрогон event_first это подтверждает
    # gate ПРОЙДЕН (коммит dcb9f5f→полировка; stage-review wf_5dafadd2 ПРОЙДЕН, blockers=0, регрессии нет,
    #   ideas_flow_ok=«поток дальше и честнее, корень суд-100% закрыт»; 468 тестов зелёных; event_first mock ОК).
    # ИЗМЕНЕНИЯ (FIXPLAN P0): #1 контракт полей node_to_facts↔_money_thesis/дело_каскада (A3=§3.1, КОРЕНЬ);
    #   #2 инверт. money-гейт + fail-open (A1=§1.1/1.2); #3 seal_spec ярус-гейт (§1.8); #4 антиманип-ключ
    #   (A6=§1.10, затрагивает мой код в.3); #5 форвард-промоушен пропускает base-rate-монетку (§1.4/2.6 —
    #   МОЙ код); #6 calibration в §11 (§1.3/1.12); #7 prob=None в гейте-270 (§1.5); #8 сырой close→adj
    #   (§5.1); #9 бюджет-гард event_first (§0.1 КРИТ).
    # ДОЛГИ из stage-review F0: [HIGH ✓ ЗАКРЫТ коммит ниже] хард-стоп бюджета — RunBudgetExceeded теперь
    #   BaseException (не глотается except Exception в openrouter/agents/_vet_money); graceful-перехват в
    #   run_funnel/run_event_first/run_masked → протокол ОСТАНОВ_бюджет; тест test_budget_hardstop (реальный complete).
    #   ОСТАЛИСЬ (не блокеры): [LOW] cascade_forward вне MONEY_EDGE_KINDS (унифицировать + assert по неклассиф. kind);
    #   [LOW] знаменатели биномтеста (n_dir vs n единообразно); [LOW] precheck-профиль event_first research-only
    #   vs vet/deep (реальный expected_calls); [LOW/env] smoke theme-guard изолировать от боевого oracle.db.
[x] F1 (P1 выдача доходит до пользователя): /goal пользователь получает связный поток идей; stage-review F1 ПРОЙДЕН
    # #10 бот мёртвая схема (mode=='live'/этап6_синтез → «идей нет», §6.1/6.2); #11 actionable §8-карточка
    #   money-идеи (A2=§6.5, метку «не рекомендация» НЕ снимать до калибр-гейта §11); #12 самопротиворечие
    #   дайджеста (A4); #13 рудимент top3 (A5); #14 дайджест режет картограф[:3] (C3); #15 «разбери ТИКЕР» (§6.5).
    # ЗАКРЫТ 2026-06-30 (тег fix-F1): коммиты 6e268c4 (#10–#15) → 4aa3952 (блокер-1: fail-closed дисклеймер +
    #   честный поток_n union) → 93716fd (блокер-2: рамка §16 переживает усечение MAX_MSG money-карточки).
    #   Гейт §24: (а) 489 зелёных, падения только theme-guard (известный LOW/env-долг F0, не из-за F1);
    #   (б) stage-review #3 wf_ef01e521-765 ПРОЙДЕН (0 находок) после двух НЕ ПРОЙДЕН — оба блокера на money-
    #   поверхности §16/§11 закрыты и покрыты тестами; (в) ideas_flow_ok положителен, mock-перепрогон чист.
    #   Codex кросс-вендор (gpt-5.1-codex-max) ПРОЙДЕН на первом диффе. Low-долги re-review (namespace дедупа
    #   union; сходимость разбивки «картограф+граф−переc»; две цифры уверенности; регистр интента «разбери»;
    #   формулировка дайджеста про суд без --vet) → отдельным проходом, НЕ блокируют. СЛЕДУЮЩИЙ: F2.
[x] F2 (P2 честность чисел и запечатывания): /goal числовое ядро без сверхуверенности/выдумок; stage-review F2 ПРОЙДЕН
    # #16 walk-forward только train (§2.1); #17 base_rate из LLM (§2.2, Инв#6); #18 изоляция R² невыровнена
    #   (§2.3); #19 объёмный p-value (§2.4); #20 calibration_band min-N (§1.7); #21 журнал без hash-chain +
    #   resolve без verify_seal (§8.1/8.2); #22 limits.yaml gates/KILL не читается (§1.11).
    # ЗАКРЫТ 2026-07-03 (тег fix-F2 на 535456e; база f2-base). Гейт: stage-review #2 wf_66331672 ПРОЙДЕН
    #   после #1 wf_9979b6f4 НЕ ПРОЙДЕН (4 блокера на §11/KILL-поверхности #22). Коммит 893736c закрыл все 4:
    #   (1) обе ветки KILL за порогом 270 (§11 «после 270»); (2) вырожденное/неизмеримое → НЕ KILL (П8);
    #   (3) edge = диагностика-прокси, не канон §11 (канон = бенчмарк §30, контур не подключён → долг FГ,
    #   Инв#4 не редактируем); (4) KILL_проверка прокинута в run/bot/devops (Инв#6, «мёртвый выход»).
    #   535456e — low-долги re-review: fail-open порога (дефолт 270), честное отображение порога.
    #   518 тестов (2 theme-guard-падения — env-долг F0). ДОЛГ FГ: каноничная edge-ветка над бенчмарком §30.
    #   СЛЕДУЮЩИЙ: F3.
[ ] F3 (P3 состязательность П10 + портфель): /goal развязка семейств гарантируется КОДОМ; stage-review F3 ПРОЙДЕН
    # #23 семейство из конфига/случайности + generator.fallback==critic (§4.1-4.4); #24 MACRO_DRIVER unmapped
    #   склеивает ставки (§7.1); #25 шок не привязан ко времени события (§5.3).
[ ] FГ (Слой Б — РЕШЕНИЯ ВЛАДЕЛЬЦА, пороги/рубрика): /goal с подписью пользователя в журнале решений
    # B1 каскадная под-рубрика; B2 cap источников≠k; B3 дедуп кластеров; B4 ускорить форвард-промоушены;
    #   B5 НЕ трогать FDR q=0.1 (терреин); фрейминг C2 — метку снять только после калибр-гейта §11.
    # НЕ кодить без явного согласия (CLAUDE.md: веса/критерии — только с подписью владельца).
[ ] FГ2 (гигиена/секреты): /goal токен femmecode из crontab вынесен+ротирован; мёртвые конфиги/код помечены
    # §9.3 plaintext-токен; §9.1 cascade_sensitivities.yaml HEADER лжёт; §3.2 build_graph test-only;
    #   §9.2 devops-петля mock+live; длинный хвост low по мере касания файлов.
